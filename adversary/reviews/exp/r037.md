# Adversary review — exp/r037

**Verdict: FAIL** (item 5: fold 2 alone supplies 51.8% of the summed gain; six of eight folds are
unchanged to six decimals). Every other item passes. This is a boundary case and it is structural:
the change only acts on 25 December, which lies inside the windows of folds 2 and 3 and no others.
The rule is applied as written; the evidence is laid out so a human can overrule it.

Branch commit: 44127c8 "exp/r037: lgbm_xmas0 — forecast 0 on Christmas Day (H031, kept vs r033)",
one commit on top of v2-loop (2b51962). Model `lgbm_xmas0`, logged r037 (WRMSSE 0.805456, spread
0.000128, verdict kept) against parent r033 `lgbm_baseline` (0.810828). Reviewed from main at
bf18b61 with main's driver and scorer, session adversary-20260913-1; src/ restored after every rerun.

Branch's own change (`git diff --stat v2-loop exp/r037`): src/features.py (+7/-1), src/model.py
(+50/-4), plus findings/r037.md, runs/detail/r037.json, one runs.csv row, one ledger row.

## 1. Look-ahead — clean

Command: `git diff main exp/r037 -- src/features.py src/model.py`

One new column, `christmas`, built in `Panel.__init__` (branch features.py line 77):
`cal["christmas"] = (cal["event_name_1"] == "Christmas").astype("int8")`, carried into
`self.calendar` and joined to every frame by `build_frame`'s existing calendar merge on `date`
(line 140). Source column is the competition calendar's `event_name_1`, known for every date in
advance; it never touches `panel.values`. `asof_features` is unchanged on this branch.

Consumers: `LGBMChristmasZero.postprocess` reads `predict["christmas"]` only, after the fit, and
sets the prediction to 0 where it is 1. `FEATURES` is unchanged (`list(FEATURE_COLUMNS)`), so the
model input is identical to the baseline; `LGBMBaseline.config()` gains no keys (`extra_config()`
returns `{}`), and r033's config hash 900159c6f3dd is untouched by the refactor. The `FEATURES` /
`extra_config` / `postprocess` hooks are refactors of `_fit_predict` and `forecast`; `_fit_predict`
selects `self.FEATURES` and restricts `categorical_feature` to those present, which for the
baseline is the same list as before.

## 2. Target leakage — none beyond the inherited sell_price assumption

The feature does not encode sales; it encodes a calendar event. The override value (0) is a
constant, not read from any sales column. Snapshot check (data/snapshot via `load_snapshot`, total
units per day): 2011-12-25 0, 2012-12-25 0, 2013-12-25 0, 2014-12-25 0 (neighbouring days
2,862-3,593). Four closed Christmases lie strictly before every fold origin (earliest origin
2015-11-23), so the rule is identifiable from pre-origin data alone. Note for the record: the
hypothesis text cites "2012-2015", i.e. it was also confirmed on 2015-12-25, which is inside the
fold 2/3 evaluation windows; that is an in-sample confirmation of a rule that needs no such
confirmation, not a data path into the forecast.

Training windows, `training_origins()` per fold: folds 1-3 have no 25 December in any training
target window (targets cover 2015-01-26..2015-12-20 at the latest); folds 4-8 contain 2015-12-25
in training. That matches the researcher's mechanism.

Inherited: horizon-week `sell_price` from the sell_prices table (features.py docstring), shared by
both sides of the comparison. Flagged, not counted.

## 3. Holdout contamination — no new references

`git grep -n holdout exp/r037 -- src/ findings/`: hits only in src/data.py and
src/score_holdout.py. `diff` of that output against the same grep on main: identical.
`git diff v2-loop exp/r037 --stat -- src/data.py src/score_holdout.py`: empty.

## 4. Frozen-file integrity — clean on the branch's own commits

`git diff v1-harness exp/r037 -- src/scorer.py src/report.py src/score_holdout.py`: empty.

`git diff v1-harness exp/r037 -- src/backtest.py`: non-empty, but every hunk is the human's
v2-loop commit (2b51962, tagged v2-loop, on main): `RUN_COLUMNS` gains `session`/`hypothesis_id`
(line 55), `run_backtest` gains `session`/`hypothesis_id` parameters and writes them to the row
(198-199, 283-284), the detail JSON gains a `seed` string (317), argparse gains `--session` /
`--hypothesis` and the researcher guard (362-375, 394-395). None of those lines are in
`make_folds` (61-74), the fold loop (219-265), or `keep_rule` (132-174).
`git diff v2-loop exp/r037 -- src/backtest.py src/scorer.py src/report.py src/score_holdout.py
src/data.py`: empty. The branch changed no frozen file. (The Makefile's own `verify-frozen`
target diffs against v2-loop for the same reason.)

## 5. Fold consistency — FAIL: gain confined to two folds, one of them over half

runs/detail/r037.json `keep_rule.no_fold_regresses` (parent r033), pass=true 8/8:

| fold | origin | r033 | r037 | delta | tolerance | share of summed gain |
|---|---|---|---|---|---|---|
| 1 | 2015-11-23 | 0.830167 | 0.830167 | +0.000000 | 0.001834 | 0.0% |
| 2 | 2015-12-07 | 0.838722 | 0.816463 | -0.022259 | 0.002539 | **51.8%** |
| 3 | 2015-12-21 | 0.854955 | 0.834237 | -0.020718 | 0.003870 | 48.2% |
| 4 | 2016-01-04 | 0.788902 | 0.788902 | +0.000000 | 0.002840 | 0.0% |
| 5 | 2016-01-18 | 0.795477 | 0.795477 | +0.000000 | 0.002668 | 0.0% |
| 6 | 2016-02-01 | 0.802840 | 0.802840 | +0.000000 | 0.001870 | 0.0% |
| 7 | 2016-02-15 | 0.789635 | 0.789635 | +0.000000 | 0.001918 | 0.0% |
| 8 | 2016-02-29 | 0.785929 | 0.785929 | +0.000000 | 0.001869 | 0.0% |

Summed gain 0.042977; fold 2 supplies 0.022259 of it, 51.8%, which is more than half. The rule in
adversary/CLAUDE.md ("FAIL ... if one fold supplies more than half the gain") therefore fires.
The substantive point behind the number: 2015-12-25 is h19 of fold 2 and h5 of fold 3 and in no
other window (checked with `make_folds`), so on six of eight folds `postprocess` is a no-op and
the branch is byte-for-byte the parent. The improvement does not "hold on all folds"; it holds on
exactly the folds that contain the one day it edits. It is a real correction of one known-closed
day, and it is not evidence of a generally better model.

## 6. Concentration — 51.4%, under the threshold

Improvement per series = weight_norm*rmsse (parent) - weight_norm*rmsse (child), mean over folds,
from runs/detail/r033.json and r037.json; volume = `actual`. Top 153 of 3,049 series by volume
(5%; 35.3% of volume, 24.2% of weight): 0.002579 of 0.005022 = **51.4%**. On the detail files' own
`contrib` column: 48.8% of 0.005372. 2,950 series improve, 0 worsen, 99 unchanged (setting a
forecast to 0 on a day whose actual is 0 cannot raise any series' error).

## 7. Noise floor — passes by a wide margin

```
git checkout exp/r037 -- src/features.py src/model.py
KEPLER_RUNS_DIR=adversary/reruns make backtest MODEL=lgbm_xmas0 SEEDS=11,99 AUTHOR=adversary SESSION=adversary-20260913-1 PARENT=r033
git checkout main -- src/features.py src/model.py
```
adversary/reruns r013: WRMSSE 0.805551, seed aggregates 11: 0.805410, 99: 0.805692, spread
**0.000282**. Harness keep rule vs r033: paired gain **+0.005277**, threshold 0.000564, 8/8 folds
within tolerance, verdict kept. Gain vs max(logged spread 0.000128, rerun spread 0.000282): 19x.

## 8. Determinism — matches

Same recipe with default seeds, adversary/reruns r014: config_hash b759aafd2f04, WRMSSE 0.805456
(spread 0.000128), WAPE 0.773236, bias 0.000651, WAPE h1-7 0.755859 / h8-14 0.768327 /
h15-28 0.784841, all six spreads identical to the logged row; all eight fold means and fold
spreads identical; per-seed aggregates 42: 0.805539, 7: 0.805418, 123: 0.805411 identical.

## Verdict reasoning

Items 1-4 and 6-8 are clean, and the mechanism is exactly what the findings file says. The FAIL
is item 5 as written: 51.8% of the summed gain sits in fold 2, and the other six folds do not move
at all. If the human reads item 5's intent as "no single fold drives the gain" rather than its
letter, this branch is a PASS with a caveat that its gain is one calendar day's worth of error
and says nothing about the model on any other day.

## Logged

runs/runs.csv row r053, model_name=exp/r037, status=rejected, author=adversary,
session=adversary-20260913-1, metrics from the item-8 rerun (adversary/reruns r014).
