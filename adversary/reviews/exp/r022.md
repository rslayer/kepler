# Adversary review — exp/r022

**Verdict: INCONCLUSIVE** (items 6 and 7 pass by thin margins; item 5 does not survive a seed change).

Branch commits: 7461189 "exp/r022: ensemble of regularised L2 (mcs200) + Tweedie LightGBM" on top of
exp/r010 (fdab107), plus 1cbae2d (bookkeeping only, code unchanged). Model `ens_mcs200_tweedie`,
logged r022 (WRMSSE 0.774048) claiming to beat r010 `lgbm_shortlags` (0.778984). Reviewed from main
at 37f2c1b with main's frozen driver and scorer; src/ restored after every rerun.

## 1. Look-ahead — clean

Command: `git diff exp/r010 exp/r022 -- src/features.py src/model.py` (r010's additions audited in
adversary/reviews/exp/r010.md, all clean).

Model under review: `EnsembleMCS200Tweedie.MEMBERS = (LGBMShortLagsMCS200, LGBMShortLagsTweedie)`,
equal weights. Both members subclass `LGBMShortLags` with the same `FEATURES`
(FEATURE_COLUMNS + lag_1..3) and change only LightGBM params (`min_child_samples=200`;
`objective=tweedie, tweedie_variance_power=1.2`). No new consumed feature.

New columns built in `asof_features` on r022, none consumed as features:
- `ever_sold`: `nz.any(axis=1)` over `hist = values[:, :origin_pos]`.
- `aux_scale`: squared first differences of `hist`, i.e. columns < origin_pos.
- `aux_wdollar`: `hist[:, -28:] * panel.price_matrix[:, origin_pos-28:origin_pos]`, both < origin_pos.
- `Panel.price_matrix`: sell price pivoted to (series x date) from the existing prices table.

New training hooks: `filter_training` (gated by `DROP_PRELAUNCH`, False on both members),
`sample_weight` (gated by `RECENCY_HALF_LIFE_DAYS=None` and `METRIC_WEIGHTS=False` on both members,
returns None), `transform_target`/`inverse_transform` (identity). Verified empirically: `lgbm_shortlags`
run under r022's src at SEED=42 reproduces r010's 0.778984 exactly (adversary/reruns r009), so the
hooks are no-ops for the reviewed members.

`Ensemble.forecast` averages member forecasts for the same origin; it introduces no data path.

## 2. Target leakage — none beyond the inherited sell_price assumption

Unchanged `y` construction and training-origin rule. Same inherited horizon-week `sell_price`
assumption as r002 and r010, shared by both sides of the comparison. Flagged, not counted.

## 3. Holdout contamination — no new references

`git grep -n holdout exp/r022 -- src/ findings/`: hits only in src/data.py and src/score_holdout.py,
byte-identical to v0-harness. `git diff main exp/r022 -- src/ findings/ | grep -i holdout`: nothing.

## 4. Frozen-file integrity — clean

`git diff v0-harness exp/r022 -- src/scorer.py src/report.py src/score_holdout.py src/backtest.py`: empty.

## 5. Fold consistency — holds at SEED=42 only

runs/detail/r010.json vs runs/detail/r022.json:

| fold | origin | r010 | r022 | delta |
|---|---|---|---|---|
| 1 | 2015-12-07 | 0.793621 | 0.788322 | -0.005299 |
| 2 | 2016-01-04 | 0.758118 | 0.750303 | -0.007815 |
| 3 | 2016-02-01 | 0.791303 | 0.785476 | -0.005827 |
| 4 | 2016-02-29 | 0.772892 | 0.772089 | -0.000803 |

All four negative at the logged seed; no single fold dominates (largest is 40% of the summed
delta). But fold 4's 0.0008 flips sign at both alternate seeds: paired against `lgbm_shortlags` at
the same seed, the ensemble is worse on fold 4 by +0.004064 (seed 7) and +0.002572 (seed 123), and
worse on fold 2 by +0.007790 at seed 7. The researcher's own keep criterion ("beats on all four
folds") is a property of seed 42.

## 6. Concentration — 58.8%, under the threshold by 1.2 points

Improvement = contrib_r010 - contrib_r022 per series (mean over folds), volume = `actual`. Top 42
series by volume (5% of 823; 30.1% of weight): 0.002902 of 0.004936 total = **58.8%** (59.2% with
41 series). Below 60%, so not INCONCLUSIVE on this item alone, but only just. 405 series improve,
372 worsen.

## 7. Noise floor — logged gain exceeds spread; paired gain does not

Commands (branch src/features.py + src/model.py, main driver/scorer):
```
KEPLER_RUNS_DIR=adversary/reruns make backtest MODEL=ens_mcs200_tweedie SEED=42 AUTHOR=adversary
KEPLER_RUNS_DIR=adversary/reruns make backtest MODEL=ens_mcs200_tweedie SEED=7 AUTHOR=adversary
KEPLER_RUNS_DIR=adversary/reruns make backtest MODEL=ens_mcs200_tweedie SEED=123 AUTHOR=adversary
```
Parent `lgbm_shortlags` at seeds 7 and 123 from the r010 review (adversary/reruns r002, r003).

| seed | ens f1 | f2 | f3 | f4 | agg | shortlags agg | paired gain |
|---|---|---|---|---|---|---|---|
| 42 | 0.788322 | 0.750303 | 0.785476 | 0.772089 | 0.774048 | 0.778984 | 0.004936 |
| 7 | 0.789111 | 0.756568 | 0.787550 | 0.771146 | 0.776094 | 0.776602 | 0.000509 |
| 123 | 0.791140 | 0.740257 | 0.789399 | 0.771346 | 0.773035 | 0.775315 | 0.002280 |

Branch seed-to-seed spread: 0.776094 - 0.773035 = **0.003058**. Logged gain **0.004936**.
Gain > spread, so not a FAIL under the checklist wording. However the paired gain at seed 7 is
0.000509, one sixth of the spread, and the mean paired gain over three seeds (0.002575) is below
the spread. The logged seed is the most favourable of the three for this branch.

## 8. Determinism — matches

SEED=42 rerun (adversary/reruns r006): 0.774048, folds 0.788322 / 0.750303 / 0.785476 / 0.772089,
WAPE 0.646837, bias -0.040383, config_hash fd6067d21d12. Identical to logged r022.

## Verdict reasoning

Every automatic-FAIL gate is clear and every threshold is met, so this is not a FAIL. It is not
graded PASS because the two quantitative gates are met by 1.2 percentage points (item 6) and
0.0019 WRMSSE (item 7), the fold-4 improvement reverses at both alternate seeds, and at seed 7
the ensemble is indistinguishable from its parent. The direction of the aggregate gain is
consistent across all three seeds, which is the strongest fact in the branch's favour. This is the
best-supported of the three branches, and it is still not established.

## Logged

runs/runs.csv row r031, model_name=exp/r022, status=rejected, author=adversary, metrics from the
SEED=42 determinism rerun.
