# Adversary review — r163 (cap511 + holiday-gated l3, H176) — VERDICT: FAIL (item 7, noise floor)

Candidate: recipe6_calendar_l2_l3g_cap511 (capacity 511 + holiday-gated store-cat aggregate feature),
m5_all holdout_mirror, vs cap511 baseline r162. Session adversary-20260921-1.

1. Look-ahead — PASS. l3_level = store-cat aggregate rolling mean over [origin-28, origin), verified equal
   to the recomputed aggregate; source indices strictly < origin. Holiday gate is pure calendar. Leak-free.
2. Target leakage — PASS. The aggregate is of sales history strictly before the origin; no target period read.
3. Holdout contamination — PASS. No holdout reference.
4. Frozen integrity — PASS. Only src/model.py (new feature class) + reconcile.py helper; scorer + keep rule
   byte-identical.
5. Fold consistency — BORDERLINE. KEPT (12/16 weighted, cond2 no-fold-regress PASS) but one fold (2013)
   supplies ~60% of the m5_all gain (>50%). The 4-fold mirror layout makes this more likely by chance.
6. Concentration — not decisive (broad-ish); superseded by item 7.
7. **Noise floor — FAIL.** Independent rerun on unseen seed 11 (m5_all mirror): cap511 0.6177 vs
   cap511+gated-l3 0.6166 -> paired gain **+0.0011**, vs the bagged 3-seed gain +0.0038 and the single-seed
   spread **0.0083**. The gain does NOT survive an untrained seed; it is within seed noise. The +0.0038 was
   partly seed-luck.
8. Determinism — PASS (deterministic pipeline).

## Verdict: FAIL (item 7). The gain is not robust to seed; do NOT score the holdout.
The holiday-gated level-3 feature gives a real DIRECTION (helps calm/spring, +0.0063 mirror screen 16/16)
but the m5_all gain (+0.0038) is within the seed noise floor (+0.0011 on unseen seed 11). Not a robust
improvement over the champion. Champion stays cap511 (holdout 0.622). Holdout shot NOT spent.
