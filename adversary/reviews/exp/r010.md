# Adversary review — exp/r010

**Verdict: INCONCLUSIVE** (item 6; item 5 does not survive a seed change).

Branch commit: fdab107 "exp/r010: short lags 1-3 at origin". Model `lgbm_shortlags`, logged r010
(WRMSSE 0.778984) claiming to beat r002 `lgbm_baseline` (0.782956). Reviewed from main at
37f2c1b with main's frozen driver and scorer; src/ restored after every rerun.

## 1. Look-ahead — clean

Command: `git diff main exp/r010 -- src/features.py src/model.py`

Columns consumed by the model: `FEATURES = FEATURE_COLUMNS + ["lag_1", "lag_2", "lag_3"]`.
In `asof_features`: `out[f"lag_{k}"] = panel.values[:, origin_pos - k]` for k in (1, 2, 3).
Column index `origin_pos - k <= origin_pos - 1`, strictly before the origin. Held constant over
the horizon via `np.repeat(values, horizon)`. Clean.

Columns built on the branch but NOT consumed by `lgbm_shortlags` (audited anyway, all clean):
- `roll_mean_{56,91,182}`: `values[:, max(origin_pos-w,0):origin_pos]`, half-open, ends at origin_pos.
- `roll_std_28`, `zero_frac_28`: `values[:, origin_pos-28:origin_pos]`.
- `days_since_sale`: from `hist = values[:, :origin_pos]`.
- `dow_mean_4w` (horizon-varying): `target_pos = origin_pos + h - 1`,
  `p = target_pos - 7*((h+6)//7)`; for h = 1..28 this gives p in [origin_pos-7, origin_pos-1];
  cols `p - 7k`, k = 0..3, max = p < origin_pos.
- `lag_364`: `src = target_pos - 364 <= origin_pos - 337`.
- `yoy_roll_7`: `[max(src-3,0), min(src+4, origin_pos))`, hi <= origin_pos - 333.
- `price_ratio_origin`, `price_rel_max`: prices with `wm_yr_wk <= week of panel.dates[origin_pos-1]`.
- `event_name`, `christmas`, `day_of_month`, `week_of_year`: calendar only.

## 2. Target leakage — none beyond the inherited sell_price assumption

Training `y` unchanged: `values[:, origin_pos:origin_pos+horizon]` with training origins
`<= fold_origin - 28` (unchanged `training_origins`). Calendar join on date and price join on
(item_id, wm_yr_wk) are unchanged from main. The only future-information channel is `sell_price`
for horizon weeks, which the contract's KNOWN ASSUMPTION declares and which the baseline r002 uses
identically, so it cannot account for a delta versus r002. Flagged, not counted.

## 3. Holdout contamination — no new references

`git grep -n holdout exp/r010 -- src/ findings/`: hits only in src/data.py and src/score_holdout.py,
both byte-identical to v0-harness (`git diff --stat v0-harness exp/r010 -- src/data.py src/score_holdout.py`
empty). `git diff main exp/r010 -- src/ findings/ | grep -i holdout`: nothing.

## 4. Frozen-file integrity — clean

`git diff v0-harness exp/r010 -- src/scorer.py src/report.py src/score_holdout.py src/backtest.py`: empty.

## 5. Fold consistency — holds at SEED=42 only

runs/detail/r002.json vs runs/detail/r010.json:

| fold | origin | r002 | r010 | delta |
|---|---|---|---|---|
| 1 | 2015-12-07 | 0.801122 | 0.793621 | -0.007501 |
| 2 | 2016-01-04 | 0.761690 | 0.758118 | -0.003572 |
| 3 | 2016-02-01 | 0.791998 | 0.791303 | -0.000694 |
| 4 | 2016-02-29 | 0.777016 | 0.772892 | -0.004124 |

All four negative at the logged seed; fold 1 carries 47% of the summed delta, so the gain is not
from one fold. Fold 3's 0.0007 is inside noise (see item 7): with the baseline rerun at the same
alternate seeds, `lgbm_shortlags` is WORSE than `lgbm_baseline` on folds 1 and 3 at seed 7
(+0.001573, +0.003669) and at seed 123 (+0.003715, +0.000967). The researcher's all-four-folds
criterion is a property of seed 42, not of the change.

## 6. Concentration — 82.1%, INCONCLUSIVE

Per-series `contrib = weight_norm * rmsse` (mean over folds) from the two detail files; improvement
= contrib_r002 - contrib_r010; volume = `actual`. Top 42 series by volume (5% of 823, holding
30.1% of weight and 34.1% of volume) account for 0.003260 of the 0.003973 total: **82.1%**
(81.5% with 41 series). Above the 60% threshold.

Why it matters: one series, FOODS_3_120_CA_1 (weight 0.032), supplies 0.00287, i.e. 72% of the
entire aggregate gain on its own. 416 series improve and 361 worsen. The change is a large gain on
a handful of high-volume series and a coin flip elsewhere; that is not evidence the feature
generalises, and the holdout weight of those few series will decide the outcome.

## 7. Noise floor — gain exceeds spread by 0.0003

Commands (branch src/features.py + src/model.py, main driver/scorer):
```
KEPLER_RUNS_DIR=adversary/reruns make backtest MODEL=lgbm_shortlags SEED=42 AUTHOR=adversary
KEPLER_RUNS_DIR=adversary/reruns make backtest MODEL=lgbm_shortlags SEED=7 AUTHOR=adversary
KEPLER_RUNS_DIR=adversary/reruns make backtest MODEL=lgbm_shortlags SEED=123 AUTHOR=adversary
# baseline at the same seeds, main src:
KEPLER_RUNS_DIR=adversary/reruns make backtest MODEL=lgbm_baseline SEED=7 AUTHOR=adversary
KEPLER_RUNS_DIR=adversary/reruns make backtest MODEL=lgbm_baseline SEED=123 AUTHOR=adversary
```

| seed | shortlags f1 | f2 | f3 | f4 | agg | baseline agg | paired gain |
|---|---|---|---|---|---|---|---|
| 42 | 0.793621 | 0.758118 | 0.791303 | 0.772892 | 0.778984 | 0.782956 | 0.003973 |
| 7 | 0.794113 | 0.748779 | 0.796437 | 0.767081 | 0.776602 | 0.782546 | 0.005943 |
| 123 | 0.792837 | 0.746997 | 0.792654 | 0.768774 | 0.775315 | 0.778892 | 0.003577 |

Branch seed-to-seed spread: 0.778984 - 0.775315 = **0.003668**. Logged gain **0.003972**.
Gain > spread, so not a FAIL under the checklist wording, but the margin is 0.0003 (8% of the
gain). The paired aggregate gain is positive at all three seeds (min 0.003577), which is the one
robust fact in this branch's favour.

## 8. Determinism — matches

SEED=42 rerun (adversary/reruns r001): 0.778984, folds 0.793621 / 0.758118 / 0.791303 / 0.772892,
WAPE 0.654607, bias -0.020766, config_hash f681bdb85d6e. Identical to logged r010.

## Verdict reasoning

Items 1-4 and 8 clean. Item 6 exceeds the threshold (82%), which the checklist grades
INCONCLUSIVE. Item 5 holds only at the logged seed. Item 7 is passed by a hair. Not proven fake,
not proven real.

## Logged

runs/runs.csv row r030, model_name=exp/r010, status=rejected, author=adversary, metrics from the
SEED=42 determinism rerun.
