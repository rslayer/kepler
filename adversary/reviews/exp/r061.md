# Adversary review — exp/r061

**Verdict: INCONCLUSIVE** (item 6: 65.5% of the net improvement is in the top 5% of series by
volume, above the 60% bar; items 7 and 8 were **not run** because the rerun command was held at the
permission prompt). No automatic-FAIL gate is tripped. The branch also contains exp/r060, which
fails item 5 (adversary/reviews/exp/r060.md).

Branch: exp/r061 = b15e132 (exp/r060) + dc08f73 "exp/r061: roll_mean_3 ported onto r060 (H042;
WRMSSE 0.804787 -> 0.803303, verdict=kept)", on main's ff837a7. Model `lgbm_xmas0_tgd_r3`, logged
r061 (WRMSSE 0.803303, spread 0.000658, verdict kept) against parent r060 `lgbm_xmas0_tgd`
(0.804787). `git merge-base --is-ancestor v1-harness exp/r061`: yes. Reviewed from main at
7d5e8a2, session adversary-20260913-3. `src/` is clean.

## 1. Look-ahead — clean

Command: `git diff exp/r060 exp/r061 -- src/features.py src/model.py`.

There is one new sales-derived column, `roll_mean_3`, built in `asof_features` by a separate loop
over `SHORT_ROLL_WINDOWS = (3,)`:
```
lo, hi = origin_pos - w, origin_pos
out[f"roll_mean_{w}"] = panel.values[:, lo:hi].mean(axis=1)
```
With w = 3 the slice covers columns origin_pos-3, origin_pos-2 and origin_pos-1. `hi` is
exclusive, so every column is strictly less than `origin_pos`. `asof_features` raises if
`origin_pos < MIN_HISTORY = 28`, so `lo >= 25`. The value is repeated across the 28 horizon rows
(`np.repeat(values, horizon)` in `build_frame`) and does not vary with horizon. Training frames use
the same function at `training_origins()`. The newest training origin is `fold_origin - 28`
(asserted `candidate + 27 < fold_origin`), so no training feature or target reaches the fold
origin. Consumer: `LGBMTgdRoll3.features = [*FEATURE_COLUMNS, "roll_mean_3"]`. `FEATURE_COLUMNS`
is untouched. The inherited Thanksgiving multiplier is audited in the r060 review.

## 2. Target leakage — none beyond the inherited price assumption

`roll_mean_3` is a function of pre-origin sales of the same series. No aggregation, no calendar
join, and no price. The `y` construction is unchanged. The price-table assumption is inherited and
shared by both sides.

## 3. Holdout contamination — no new references

`git grep -n holdout exp/r061 -- src/ findings/`: the same 38 hits in src/adapters/, src/data.py,
src/evaluate.py and src/score_holdout.py as on main. The branch changes only src/features.py and
src/model.py under src/. findings/r060.md and r061.md have no hit.

## 4. Frozen-file integrity — clean

`git diff main exp/r061 --stat -- src/backtest.py src/scorer.py src/report.py src/score_holdout.py
src/data.py src/contract.py Makefile tools/ CLAUDE.md`: empty. `git diff v1-harness exp/r061 --
src/scorer.py src/report.py src/score_holdout.py` (135 lines) and `-- src/backtest.py` (98 lines)
match main line for line. They come from human commits 2b51962 and 91051c7.

## 5. Fold consistency — passes the harness rule; sign flips across seeds

runs/detail/r061.json `keep_rule` (parent r060): paired_gain pass (gain 0.001484, threshold
0.001316, parent spread 0.000139, child spread 0.000658), no_fold_regresses pass 8/8,
bias_guardrail pass.

| fold | origin | r060 | r061 | delta | tolerance | share of summed gain |
|---|---|---|---|---|---|---|
| 1 | 2015-11-23 | 0.824813 | 0.823604 | -0.001209 | 0.003409 | 10.2% |
| 2 | 2015-12-07 | 0.816463 | 0.814159 | -0.002305 | 0.002539 | 19.4% |
| 3 | 2015-12-21 | 0.834237 | 0.835075 | +0.000839 | 0.004992 | -7.1% |
| 4 | 2016-01-04 | 0.788902 | 0.785411 | -0.003491 | 0.002840 | 29.4% |
| 5 | 2016-01-18 | 0.795477 | 0.794178 | -0.001299 | 0.002668 | 10.9% |
| 6 | 2016-02-01 | 0.802840 | 0.801558 | -0.001282 | 0.003489 | 10.8% |
| 7 | 2016-02-15 | 0.789635 | 0.790515 | +0.000880 | 0.001918 | -7.4% |
| 8 | 2016-02-29 | 0.785929 | 0.781924 | -0.004004 | 0.001869 | **33.7%** |

The largest fold share is 33.7%, so this is not a FAIL. Per-seed fold deltas from the `seeds`
blocks (child minus parent, same seed):

| seed | f1 | f2 | f3 | f4 | f5 | f6 | f7 | f8 | agg gain |
|---|---|---|---|---|---|---|---|---|---|
| 42 | -0.000393 | -0.003305 | -0.000233 | -0.003855 | -0.002171 | -0.000928 | +0.001146 | -0.003959 | +0.001712 |
| 7 | -0.000931 | -0.001574 | +0.001529 | -0.004369 | -0.000113 | +0.000808 | +0.000781 | -0.004624 | +0.001062 |
| 123 | -0.002305 | -0.002035 | +0.001220 | -0.002248 | -0.001614 | -0.003725 | +0.000712 | -0.003430 | +0.001678 |

Folds 3 and 6 change sign between seeds. Fold 7 is worse at every seed. Folds 1, 2, 4, 5 and 8
improve at all three logged seeds.

**Same end state as a discarded run.** r061's eight fold means are identical to six decimals to
r050 (`lgbm_xmas0_r3_tgd`, r044 + the same override): [0.823604, 0.814159, 0.835075, 0.785411,
0.794178, 0.801558, 0.790515, 0.781924] in both runs/detail files. r050 was **discarded** against
r044 (paired gain 0.000758 < threshold 0.001316). The chain r037 -> r060 -> r061 reaches that
same model by applying the override first, against a quiet parent, and roll_mean_3 second. The
findings file says so openly. The fact for the human: the harness has kept and discarded this same
model, depending only on the order of the steps.

## 6. Concentration — 65.5%, INCONCLUSIVE

Improvement per series = weight_norm x rmsse (r060) - weight_norm x rmsse (r061), from the
`per_series` blocks (jq join on id). Volume = parent `actual`. Top 153 of 3,049 series by volume
(5%, 35.3% of volume): 0.000655 of 0.001000 net = **65.5%** (top 152: 65.3%). On the `contrib`
column: 61.0% of 0.001484. Both bases are above 60%. 1,431 series improve, 1,532 worsen and 86 are
unchanged. Gross +0.003473 against -0.002473, so the net is 29% of the gross positive. Inside the
top 153 the split is +0.001982 / -0.001327. This matches r044's item-6 finding (65.9% / 61.1%), as
expected: folds 2-8 are the same model change.

## 7. Noise floor — NOT RUN (blocked); prior independent evidence only

Not attempted separately. The identical recipe for exp/r060
(`KEPLER_RUNS_DIR=adversary/reruns make backtest ... SEEDS=11,99`) was held at the permission
prompt earlier in this session and did not execute.

Logged: the paired gain of 0.001484 is 2.26x the logged spread of 0.000658. There is no rerun
spread. Per seed it is +0.001712 / +0.001062 / +0.001678. Seed 7's same-seed gain is 1.6x the
spread.

Nearest independent evidence, not a substitute: on folds 2-8, r061 vs r060 is exactly r044 vs r037,
and the override only touches fold 1. adversary-20260913-1 reran that pair at seeds 11,99 under
the v2 harness (adversary/reruns r013 vs r015). The same-seed gain was +0.001429 (11: +0.001579,
99: +0.001278), the rerun spread 0.000583, and **the harness discarded r015 on fold 1** (+0.004270
vs tolerance 0.003805). Fold 1 here includes the override, so that fold-1 result does not carry
over directly. The v2 to v3 port reproduced r037 and r044 exactly at the default seeds, which has
not been checked at 11,99.

## 8. Determinism — NOT RUN (blocked)

Not verified.

## Verdict reasoning

The feature is leak-free and no gate fails. The gain is not established as real. 65% of it sits in
the top 5% of series by volume. It is about 2x the seed spread and 0.00017 above the harness
threshold, and two folds change sign between seeds. The same model was discarded as r050 when the
steps ran in the other order. The mandatory reruns (7, 8) were not done. INCONCLUSIVE.

## Logged

runs/runs.csv row r075, model_name=exp/r061, status=rejected, author=adversary,
session=adversary-20260913-3. Metric columns are empty because the item-8 rerun did not run.
