# Adversary review — r147 (leaves511, capacity lever) — VERDICT: PASS

Candidate: `recipe_search` with `{num_leaves: 511}` (= recipe6_calendar_l2 + higher capacity),
m5_all, v9 folds, bagged. Parent: r145 (recipe v9 m5_all baseline). Reviewed on main (candidate
was run on main via the confirmation chain, not an exp/* branch). Session adversary-20260919-1.

1. **Look-ahead — PASS.** The only change vs the champion is `num_leaves 127->511`. Feature
   columns are byte-identical to recipe6_calendar_l2 (verified: recipe_search.features ==
   champion.features). No new feature reads panel.values, so no new column can reach the origin;
   the champion's leak-free contract is inherited unchanged.
2. **Target leakage — PASS.** No new features; nothing new can encode the target.
3. **Holdout contamination — PASS.** `grep holdout runs/search/cfg_leaves511.json` = none; the
   config is `{num_leaves:511}` only. No tool in the path references holdout/.
4. **Frozen-file integrity — PASS.** scorer.py/scorer_hier.py/score_holdout.py/scoring.py byte-
   identical across the v9 harness change; keep_rule in backtest.py byte-identical to v9-yearround
   (58 lines, 0 diff). The v9 fold-layout change (spacing 91) applies equally to parent and child,
   so the comparison is fair.
5. **Fold consistency — PASS.** keep_rule cond2 (no-fold-regress) PASS 8/8. Per-fold gain vs r145:
   +0.0173,-0.0006,+0.0173,+0.0148,+0.0052,+0.0202,+0.0092,+0.0302. Max single fold = +0.0302 =
   27% of the +0.1137 total (< 50%). The one negative fold (-0.0006) is within tolerance.
6. **Concentration — PASS.** Top 5% of series by volume account for -3.1% of the per-series
   improvement (not concentrated); 56% of series improve. The gain is broad and lives on the
   aggregate levels (as WRMSSE is designed to reward), not a few high-volume series.
7. **Noise floor — PASS.** Independent rerun on unseen seed 11 (m5_all): leaves511 hier 0.65341
   vs champion baseline 0.66629 -> paired gain **+0.01288**, which is **~3x** the logged 3-seed
   spread (0.004353). The gain holds on a seed the model never trained with; not seed-fragile.
   (Reruns in adversary/reruns/: r019 baseline@11, r020 leaves511@11.)
8. **Determinism — PASS.** The v9 parallel path was proven bit-identical to serial (r148 vs r134,
   diff 0.00e+00 on every fold); with fixed seeds and num_threads the pipeline is deterministic,
   so r147's logged metrics reproduce.

## Verdict: PASS — no reason found that the improvement is not real.
Capacity (num_leaves 511) is a genuine, broad, seed-robust, leak-free gain over the champion on
the strict m5_all gate (+0.0143 hier three-seed; +0.0129 on unseen seed 11). Cleared for the
human holdout decision. The adversary does NOT score the holdout.
