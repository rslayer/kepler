# Adversary review — exp/r070

**Verdict: INCONCLUSIVE** (items 7 and 8 are mandatory and were **not run**: the rerun command
was held at the permission prompt and not approved this session). Items 1-6 pass on the branch's
own change. The branch also carries exp/r060 (FAIL, item 5) and exp/r061 (INCONCLUSIVE, item 6).
Merging or promoting exp/r070 brings both in.

Branch: exp/r070 = b15e132 (r060) + dc08f73 (r061) + 5219469 "exp/r070: 80 training origins with
120-day recency half-life on r061 (H023; WRMSSE 0.803303 -> 0.798186, verdict=kept)", plus 44ffa4c
and 31960b2 (bookkeeping for r071-r073 only: `git diff 5219469 exp/r070 --stat -- src/` is empty).
Model `lgbm_xmas0_tgd_r3_o80hl120`, logged r070 (WRMSSE 0.798186, spread 0.001385, 667 s, verdict
kept) against parent r061 `lgbm_xmas0_tgd_r3` (0.803303). `git merge-base --is-ancestor v1-harness
exp/r070`: yes. Reviewed from main at 7d5e8a2, session adversary-20260913-3. `src/` is clean.

## 1. Look-ahead — clean

Command: `git diff exp/r061 exp/r070 -- src/features.py src/model.py` (model.py +38 only).

No feature changes. `LGBMO80HalfLife` sets `N_TRAIN_ORIGINS = 80` and overrides `_fit_predict` to
pass a sample weight. Every other line of `_fit_predict` is the baseline's.
- Training origins: `training_origins(panel, fold_origin, n_origins=80, spacing_days=7)`. The newest
  is `fold_origin - 28` and the i-th is `fold_origin - 28 - 7i`. `assert candidate + 27 <
  fold_origin` holds for each, so every training target window closes before the fold origin.
  Each frame's as-of features read `< candidate_pos` (the r061 review covers `roll_mean_3`). The
  oldest origin for fold 1 (2015-11-23) is 2015-10-26 - 553 d = 2014-04-21, well past
  MIN_HISTORY, so the loop does not break early.
- Weights: `age = np.arange(n_origins) * 7`, `weight = np.repeat(0.5 ** (age / 120),
  rows_per_origin)`. They depend only on the origin's position in the list, not on any data.
  `build_training_set` concatenates `[build_frame(o) for o in origins]` in `training_origins`
  order, which is newest first, so index 0 = age 0 is correct. Each frame has n_series x 28 rows
  (dense pivot, left merge on unique calendar dates). `rows_per_origin = len(predict)` equals that,
  and the block assert passes in the logged run.

## 2. Target leakage — none beyond the inherited price assumption

No new column. The longer window puts last year's holiday season (Thanksgiving and Christmas 2014)
into the training targets. Those are pre-origin sales and a legitimate training signal, not
leakage. One note for interpretation: in fold 1 the model now sees Thanksgiving 2014 in training
and the inherited r060 multiplier still scales Thanksgiving 2015. The fold-1 change here is the
smallest of the holiday folds (-0.0015, +0.0018 at seed 123). The price-table assumption is
inherited and shared.

## 3. Holdout contamination — no new references

`git grep -n holdout exp/r070 -- src/ findings/`: the same 38 hits in src/adapters/, src/data.py,
src/evaluate.py and src/score_holdout.py as on main. `git grep -c holdout exp/r070 -- findings/`:
no hit in findings/r060-r073.

## 4. Frozen-file integrity — clean

`git diff main exp/r070 --stat -- src/backtest.py src/scorer.py src/report.py src/score_holdout.py
src/data.py src/contract.py Makefile tools/ CLAUDE.md`: empty. `git diff v1-harness
exp/r070 -- src/scorer.py src/report.py src/score_holdout.py` (135 lines) and `-- src/backtest.py`
(98 lines) match main. They come from human commits 2b51962 (v2-loop) and 91051c7 (v3-engine), and
`git diff v3-engine main` on those files is empty.

## 5. Fold consistency — passes; the gain is holiday-fold-weighted

runs/detail/r070.json `keep_rule` (parent r061): paired_gain pass (gain 0.005117, threshold
0.002770, parent spread 0.000658, child spread 0.001385), no_fold_regresses pass 8/8,
bias_guardrail pass (|bias| 0.004547 -> 0.008983, guardrail +0.02).

| fold | origin | r061 | r070 | delta | tolerance | share of summed gain |
|---|---|---|---|---|---|---|
| 1 | 2015-11-23 | 0.823604 | 0.822126 | -0.001478 | 0.003409 | 3.6% |
| 2 | 2015-12-07 | 0.814159 | 0.803807 | -0.010352 | 0.003400 | 25.3% |
| 3 | 2015-12-21 | 0.835075 | 0.819501 | -0.015575 | 0.004992 | **38.0%** |
| 4 | 2016-01-04 | 0.785411 | 0.776653 | -0.008758 | 0.000951 | 21.4% |
| 5 | 2016-01-18 | 0.794178 | 0.790622 | -0.003555 | 0.001688 | 8.7% |
| 6 | 2016-02-01 | 0.801558 | 0.799755 | -0.001804 | 0.003489 | 4.4% |
| 7 | 2016-02-15 | 0.790515 | 0.789754 | -0.000761 | 0.003873 | 1.9% |
| 8 | 2016-02-29 | 0.781924 | 0.783272 | +0.001348 | 0.002896 | -3.3% |

The largest single fold is 38.0%, so this is not a FAIL. Folds 2-4, whose windows contain
Christmas and New Year, supply 84.7% of the summed gain. Per-seed fold deltas (child minus parent,
same seed):

| seed | f1 | f2 | f3 | f4 | f5 | f6 | f7 | f8 | agg gain |
|---|---|---|---|---|---|---|---|---|---|
| 42 | -0.002677 | -0.009176 | -0.013213 | -0.008109 | -0.004429 | -0.002059 | -0.003497 | +0.000703 | +0.005307 |
| 7 | -0.003605 | -0.010394 | -0.016769 | -0.009875 | -0.004077 | -0.004959 | +0.001450 | -0.000116 | +0.006043 |
| 123 | +0.001848 | -0.011485 | -0.016742 | -0.008291 | -0.002160 | +0.001607 | -0.000236 | +0.003455 | +0.004001 |

Folds 2-5 improve at every seed, and by large margins on 2-4. Folds 1, 6, 7 and 8 change sign
between seeds. Fold 8 is worse at two of three seeds (+0.0035 at seed 123). The findings file's
"first long-history variant that does not lose February" holds on the three-seed mean. At seed 123
it loses folds 6 and 8.

## 6. Concentration — 52.7%, under the threshold

Improvement per series = weight_norm x rmsse (r061) - weight_norm x rmsse (r070), from the
`per_series` blocks (jq join on id). Volume = parent `actual`. Top 153 of 3,049 series by volume
(5%, 35.3% of volume): 0.002912 of 0.005521 net = **52.7%** (top 152: 52.9%). On the `contrib`
column: 51.5% of 0.005117. 1,485 series improve, 1,478 worsen and 86 are unchanged. Gross
+0.008535 against -0.003013. Inside the top 153 the split is +0.004102 / -0.001190.

## 7. Noise floor — NOT RUN (blocked); logged evidence only

Recipe (not executed for this branch; the identical form for exp/r060 was held at the permission
prompt this session):
```
git checkout exp/r070 -- src/features.py src/model.py
KEPLER_RUNS_DIR=adversary/reruns make backtest MODEL=lgbm_xmas0_tgd_r3_o80hl120 DATASET=m5_ca1 SEEDS=11,99 AUTHOR=adversary SESSION=adversary-20260913-3 PARENT=r061
git checkout main -- src/features.py src/model.py
```
Logged, not independent: the paired gain of 0.005117 is 3.7x the logged child spread of 0.001385.
Same-seed gains are +0.005307 / +0.006043 / +0.004001, and the smallest is 2.9x the spread. The
spread doubled against the parent because of seed 123 (aggregate 0.799048 vs 0.797846 and
0.797663). The evidence points to a gain above the noise floor, but the independent-seed check is
what item 7 requires, and it was not done.

## 8. Determinism — NOT RUN (blocked)

Not verified. The 80-origin fit changes row counts and adds sample weights. Neither is a known
source of nondeterminism under `deterministic=True, force_row_wise=True, num_threads=4`, but that
is not a rerun.

## Other observations (not checklist gates)

- **Two knobs, one run.** 80 origins and the 120-day half-life move together. Ledger H023 defines
  them as one hypothesis. 80 origins alone is H005, discarded on r037 (r038). A 120-day half-life on
  40 origins was never run, so the run does not isolate which knob carries the gain. With a 120 d
  half-life, origins 40-79 get weights 0.20-0.04, so the second half of the window adds little
  weight but carries last year's holidays.
- **Chain.** The keep rule compares against r061. The r061 base is INCONCLUSIVE and the r060 base
  FAILs item 5. If r060 is not overruled, this increment has not been measured on an acceptable
  base.

## Verdict reasoning

The branch's own change is leak-free and touches no frozen file. The gain is large relative to the
logged spread, not concentrated in the top series, and not dominated by one fold, although 85% of
it is the Christmas/New Year folds and folds 1, 6, 7 and 8 change sign by seed. Items 7 and 8 are
mandatory and were not run, so PASS cannot be given. INCONCLUSIVE, pending the two reruns above
and a human decision on exp/r060.

## Logged

runs/runs.csv row r076, model_name=exp/r070, status=rejected, author=adversary,
session=adversary-20260913-3. Metric columns are empty because the item-8 rerun did not run.
