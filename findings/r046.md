# r046 — lgbm_xmas0_r3_dom

**Author:** researcher (session researcher-20260912-1)

**Hypothesis:** H035 — `day_of_month` as a calendar-known, horizon-varying feature on top of
r044 (SNAP days in CA fall on days 1-10 and paycheck cycles on the 1st/15th; the snap flag
alone cannot separate within-month position). First component of the H029 bundle, run alone.

**What changed:** `src/features.py`: `Panel.__init__` adds calendar column `day_of_month`
(verified equal to the date's day for every row; lgbm_xmas0 and lgbm_xmas0_r3 config hashes
unchanged). `src/model.py`: new class `LGBMXmasDayOfMonth` (`lgbm_xmas0_r3_dom`) = r044
FEATURES + `day_of_month`.

**Result vs current best (r044, 0.804061):** WRMSSE 0.803993 (spread 0.001046), paired gain
+0.000068: neutral. Per fold (r044 -> r046): 1 0.8297 -> 0.8288 (-0.0009), 2 0.8142 ->
0.8137 (-0.0005), 3 0.8351 -> 0.8339 (-0.0012), 4 0.7854 -> 0.7843 (-0.0011), 5 0.7942 ->
0.7943 (+0.0002), 6 0.8016 -> 0.8034 (+0.0018), 7 0.7905 -> 0.7905 (-0.0001), 8 0.7819 ->
0.7831 (+0.0012). Bias +0.0007 -> -0.0071 (fold 4 -0.047: the January origin's within-month
pattern is not the training months' pattern). WAPE h1-7 0.7534 -> 0.7513. Keep rule: 1 FAIL,
2 PASS (8/8), 3 PASS. Detail in runs/detail/r046.json.

Within-month position adds nothing the snap flag and dow do not already give; the bias it
introduces on fold 4 argues against carrying it.

**Verdict:** discard. Change reverted.
