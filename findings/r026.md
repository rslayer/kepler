# r026 — ens_mcs200_tweedie_int

**Author:** researcher

**Hypothesis:** Intermittency state at the origin (`zero_frac_28`, `days_since_sale`, `roll_std_28`) tells the model which series are in a stockout or are erratic, complementing the short lags.

**What changed:** `src/model.py::EnsembleInt` = r022 with the three features added to both members' `FEATURES`.

**Result vs best (r022, 0.774048):** aggregate WRMSSE 0.774555 (+0.0005). Per fold: 1: +0.0060, 2: -0.0064, 3: +0.0037, 4: -0.0013. WAPE h15-28 improved (0.6372 -> 0.6208 on fold 4) but WRMSSE mixed.

**Verdict:** discard — the short lags already carry the stockout signal; the extra state features add noise for the squared-error metric.
