# r005 — lgbm_dowmean

**Author:** researcher

**Hypothesis:** The baseline only sees `dow` plus origin-anchored lags, so the weekly shape at horizons 8-28 is learned only through the global dow effect. A per-series same-weekday mean over the 4 weeks before the origin (`dow_mean_4w`, evaluated for the target date's weekday, reads only columns < origin) should cut mid/long-horizon error.

**What changed:** `src/model.py::LGBMDowMean` = baseline + feature `dow_mean_4w` (built in `src/features.py::horizon_features`).

**Result vs best (r002, 0.782956):** aggregate WRMSSE 0.779102 (-0.0039). Per fold: 1: -0.0049, 2: -0.0076, 3: **+0.0020**, 4: -0.0048. WAPE h8-14 0.6516 -> 0.6492; h15-28 flat.

**Verdict:** investigate — fails the all-four-folds rule because of fold 3. Feature is directionally useful; retry on top of a stronger base (e.g. Tweedie) later.
