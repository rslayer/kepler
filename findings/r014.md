# r014 — lgbm_sl_dowmean

**Author:** researcher

**Hypothesis:** Retry of r005's same-weekday mean (`dow_mean_4w`) on top of the kept short lags (r010).

**What changed:** `src/model.py::LGBMShortLagsDow` = r010 + `dow_mean_4w`.

**Result vs best (r010, 0.778984):** aggregate WRMSSE 0.775933 (-0.0031). Per fold: 1: +0.0007, 2: -0.0125, 3: +0.0028, 4: -0.0032.

**Verdict:** discard — mixed on folds twice now (r005, r014); the feature's signal is real on fold 2 only.
