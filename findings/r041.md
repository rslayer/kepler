# r041 — lgbm_xmas0_mcs200

**Author:** researcher (session researcher-20260912-1)

**Hypothesis:** H012 retest under the v1 harness — `min_child_samples` 200 instead of 50 on
r037. v0 r018 gained 0.0048 aggregate at one seed and lost 0.0005 on one fold; the priors
list it as cheap to retest under three seeds.

**What changed:** `src/model.py`: new class `LGBMXmasMCS200` (`lgbm_xmas0_mcs200`), a
subclass of `LGBMChristmasZero` with `PARAMS["min_child_samples"] = 200`. Features and
origins identical to r037.

**Result vs current best (r037, 0.805456):** WRMSSE 0.805487 (spread 0.000976), paired gain
-0.000031: neutral to five decimals. Per fold (r037 -> r041): 1 0.8302 -> 0.8291 (-0.0010),
2 0.8165 -> 0.8194 (+0.0029), 3 0.8342 -> 0.8342 (0.0000), 4 0.7889 -> 0.7881 (-0.0008),
5 0.7955 -> 0.7940 (-0.0014), 6 0.8028 -> 0.8028 (0.0000), 7 0.7896 -> 0.7900 (+0.0003),
8 0.7859 -> 0.7862 (+0.0003). Bias +0.0007 -> -0.0028. Keep rule: 1 paired gain FAIL,
2 no fold regress PASS, 3 bias PASS. Detail in runs/detail/r041.json.

On the all-departments v1 subset (3,049 series, ~3.4M training rows) the leaf-size
regulariser that helped v0's FOODS_3 subset does nothing; the v0 gain was seed noise, as the
adversary's reseeding suggested. It does raise seed spread (0.0001 -> 0.0010).

**Verdict:** discard. Change reverted.
