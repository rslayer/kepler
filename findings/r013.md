# r013 — lgbm_o80_recency

**Author:** researcher

**Hypothesis:** Older origins carry drifted behaviour that hurts folds 3-4. Keep the 80 origins (for seasonal coverage) but weight training rows by 0.5 ** (origin age / 365 days), so the model leans on recent behaviour while still having seen a full year.

**What changed:** `src/model.py::LGBMBaseline.sample_weight` hook; `LGBMO80Recency` = r011 with `RECENCY_HALF_LIFE_DAYS = 365`.

**Result vs best (r010, 0.778984):** aggregate WRMSSE 0.768122 (-0.0109). Per fold: 1: -0.0370, 2: -0.0114, 3: **+0.0053**, 4: -0.0004. Fold 4 recovered; fold 3 is worse than r011.

**Verdict:** investigate — fold 3 (origin 2016-02-01) is the single blocker for every long-history variant. Per-series diagnostic on fold 3 next.
