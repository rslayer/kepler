# r039 — lgbm_xmas0_yoy

**Author:** researcher (session researcher-20260912-1)

**Hypothesis:** H028 — horizon-aligned year-ago features. For each forecast date d the model
sees sales at d-364 (`lag_364`, same weekday) and the mean over d-364 +/- 3 days
(`yoy_roll_7`), so Thanksgiving/Black Friday and December seasonality that no 40-origin
training window contains are visible at forecast time.

**What changed:** `src/features.py`: new `yearago_features(panel, origin_pos, horizon)`
(reads positions <= origin_pos + 27 - 361, i.e. >= 334 days before the origin; asserted;
alignment verified against the raw panel at the fold-1 origin, no NaNs at any training
origin), called from `build_frame`. `src/model.py`: new class `LGBMXmasYearAgo`
(`lgbm_xmas0_yoy`) = r037 FEATURES + `lag_364`, `yoy_roll_7`.

**Result vs current best (r037, 0.805456):** WRMSSE 0.802612 (spread 0.000768), paired gain
+0.002844 against a threshold of 0.001536 (PASS). Per fold (r037 -> r039):
1 0.8302 -> 0.8169 (-0.0133), 2 0.8165 -> 0.8047 (-0.0117), 3 0.8342 -> 0.8206 (-0.0136),
4 0.7889 -> 0.7846 (-0.0043), 5 0.7955 -> 0.7959 (+0.0004), 6 0.8028 -> 0.8082 (+0.0054),
7 0.7896 -> 0.7963 (+0.0067), 8 0.7859 -> 0.7936 (+0.0076). Fold-1 bias +0.053 -> +0.036.
WAPE h15-28 0.7848 -> 0.7787. Keep rule: 1 PASS, 2 no fold regress FAIL (folds 6-8 regress,
worst fold 8 +0.007630 vs tolerance 0.001869), 3 bias PASS. Detail in runs/detail/r039.json.

Same shape as r038 (80 origins): the holiday folds 1-3 gain ~0.012-0.014 each, the February
folds 6-8 lose 0.005-0.008. Both changes hand the model last year's absolute level for the
target window; in February that level is stale (fold 6-8 bias moves further negative). The
natural next step is a year-ago SHAPE feature rescaled to the current level (H032).

**Verdict:** discard. Change reverted.
