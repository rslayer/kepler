# r004 — lgbm_baseline (refactor determinism check)

**Author:** researcher

**Hypothesis:** Refactoring `LGBMBaseline` so experiments can subclass it (class-level `FEATURES`, target-transform hooks, categorical alignment over any categorical feature) and adding extra feature builders to `src/features.py` (computed for every frame, consumed only by models that list them) does not change the baseline.

**What changed:** `src/model.py::LGBMBaseline._fit_predict` reads `self.FEATURES` instead of the module global; `src/features.py::asof_features`, new `horizon_features`, `build_frame` compute extra columns (`roll_mean_56/91/182`, `roll_std_28`, `zero_frac_28`, `days_since_sale`, `dow_mean_4w`, `lag_364`, `yoy_roll_7`, `price_ratio_origin`, `price_rel_max`, `event_name`, `christmas`, `day_of_month`, `week_of_year`). `FEATURE_COLUMNS` is untouched.

**Result:** WRMSSE 0.782956, identical to r002/r003 on every fold and every metric. Config hash unchanged (7e7076451979).

**Verdict:** keep — pure refactor, baseline reproduced to six decimals.
