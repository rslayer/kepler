# r011 — lgbm_shortlags_o80

**Author:** researcher

**Hypothesis:** The r008 gain from 80 origins should stack on r010's short lags, and the short-lag state features might offset the fold 3/4 dilution seen in r008.

**What changed:** `src/model.py::LGBMShortLagsOrigins80` = r010 with `N_TRAIN_ORIGINS = 80`.

**Result vs best (r010, 0.778984):** aggregate WRMSSE 0.766310 (-0.0127). Per fold: 1: -0.0398, 2: -0.0148, 3: **+0.0015**, 4: **+0.0023**. Same shape as r008: the extra history is a large win on the winter folds and a small, consistent loss on folds 3-4.

**Verdict:** investigate — fails the all-folds rule again by ~0.002 on folds 3/4. The loss is stable across r008/r011, so it is structural (older origins carry something harmful), not noise. Next: identify and remove the harmful rows (pre-launch zeros) or down-weight old origins.
