# r019 — lgbm_sl_nomonth

**Author:** researcher

**Hypothesis:** With 40 weekly origins the training targets span ~9 months, so the horizon's calendar month is often unseen (January for fold 1) and the `month` split is noise. Removing it should help.

**What changed:** `src/model.py::LGBMShortLagsNoMonth` = r010 minus `month`.

**Result vs best (r010, 0.778984):** aggregate WRMSSE 0.784063 (+0.0051). Per fold: 1: +0.0141, 2: -0.0052, 3: +0.0074, 4: +0.0040.

**Verdict:** discard — `month` carries real signal even with partial coverage (fold 1 relies on it to separate December).
