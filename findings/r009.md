# r009 — lgbm_origins60

**Author:** researcher

**Hypothesis:** r008 (80 origins) won big on folds 1-2 and lost slightly on folds 3-4. 60 origins (~420 days) still covers a full year for every fold but reaches back less far, so it may keep the winter gain while not diluting recent behaviour.

**What changed:** `src/model.py::LGBMOrigins60` = baseline with `N_TRAIN_ORIGINS = 60`.

**Result vs best (r002, 0.782956):** aggregate WRMSSE 0.775040 (-0.0079). Per fold: 1: -0.0272, 2: -0.0181, 3: **+0.0097**, 4: +0.0039. Worse than r008 on folds 3 and 4, so the fold 3/4 loss is not monotone in the amount of history; it depends on which periods enter the training window.

**Verdict:** discard — fails the rule and is dominated by r008. Diagnostic run alongside: with 40 origins the training targets never include January, so `month=1` is unseen for fold 1's last days and forecasts decay; and the shortest lag is 7, so an ongoing stockout at the origin is invisible to the model.
