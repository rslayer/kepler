# r015 — lgbm_sl_price

**Author:** researcher

**Hypothesis:** Price drops drive spikes. `price_ratio_origin` (target-week price / last price known at the origin) and `price_rel_max` (target-week price / item max price up to the origin) encode promo depth, which the raw `sell_price` level cannot express across items.

**What changed:** `src/model.py::LGBMShortLagsPrice` = r010 + `price_ratio_origin`, `price_rel_max` (built in `src/features.py::build_frame`).

**Result vs best (r010, 0.778984):** aggregate WRMSSE 0.777172 (-0.0018). Per fold: 1: +0.0058, 2: -0.0094, 3: +0.0007, 4: -0.0043. WAPE improved in all buckets (0.6546 -> 0.6532) but WRMSSE mixed.

**Verdict:** discard — fails the rule; fold 1 (Christmas window) is worse, probably because holiday price changes there mean something different.
