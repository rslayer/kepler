# r047 — lgbm_xmas0_r3_evt

**Author:** researcher (session researcher-20260912-1)

**Hypothesis:** H036 — historical event effect on top of r044: for each horizon date that
carries `event_name_1`, the store-level ratio of that event's sales to the mean of the
surrounding +/-7 days, averaged over prior occurrences whose window closes before the
origin (1.0 on non-event days). Fold 1's +0.05 bias sits in Thanksgiving week and no
40-origin training window contains a Thanksgiving; the February folds carry Super Bowl,
Valentine's and Presidents' Day.

**What changed:** `src/features.py`: `Panel` gains `event_name` (by position) and
`store_total`; new `event_effect_features(panel, origin_pos, horizon)` (reads store totals
only at positions < origin_pos, asserted; verified invariant to zeroing the future), added to
`build_frame` as `event_effect`. At the fold-1 origin: Thanksgiving 0.598, Chanukah End 1.004;
at the fold-6 origin: SuperBowl 1.208, LentStart 0.805, ValentinesDay 0.851, PresidentsDay
1.054. `src/model.py`: new class `LGBMXmasEventEffect` (`lgbm_xmas0_r3_evt`) = r044 FEATURES
+ `event_effect`. Config hashes of lgbm_xmas0 / lgbm_xmas0_r3 unchanged.

**Result vs current best (r044, 0.804061):** WRMSSE 0.803627 (spread 0.000853), paired gain
+0.000434 against a threshold of 0.001706. Per fold (r044 -> r047): 1 0.8297 -> 0.8296
(-0.0001), 2 0.8142 -> 0.8130 (-0.0012), 3 0.8351 -> 0.8314 (-0.0037), 4 0.7854 -> 0.7851
(-0.0003), 5 0.7942 -> 0.7960 (+0.0018), 6 0.8016 -> 0.8024 (+0.0008), 7 0.7905 -> 0.7887
(-0.0018), 8 0.7819 -> 0.7829 (+0.0010). Fold-1 bias unchanged at +0.055. Keep rule: 1 FAIL,
2 FAIL (fold 5 +0.001782 vs tolerance 0.001759), 3 PASS. Detail in runs/detail/r047.json.

The event-day effect is real but tiny in WRMSSE terms (one day of 28), and it does not touch
fold 1's error: that fold over-forecasts the whole window (h1-7 WAPE 0.865 vs ~0.72-0.79
elsewhere, bias +0.055 with or without the Thanksgiving ratio), so the problem is a level shift
across the horizon that the origin's lags cannot see, not the holiday itself.

**Verdict:** discard. Change reverted.
