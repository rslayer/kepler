# r006 — lgbm_longroll

**Author:** researcher

**Hypothesis:** Long-horizon error (h15-28 is the worst bucket) comes partly from the model anchoring on a noisy 7/28-day level. Longer as-of-origin windows (`roll_mean_56/91/182`) give a steadier series level.

**What changed:** `src/model.py::LGBMLongRoll` = baseline + `roll_mean_56`, `roll_mean_91`, `roll_mean_182` (built in `src/features.py::asof_features`).

**Result vs best (r002, 0.782956):** aggregate WRMSSE 0.786299 (+0.0033), worse on all four folds (fold 1 +0.0096, fold 1 h15-28 WAPE 0.797 -> 0.850). WAPE worse in every bucket.

**Verdict:** discard — long windows hurt; with an L2 objective they seem to pull forecasts toward stale levels, and fold 1 (Christmas window) suffers most.
