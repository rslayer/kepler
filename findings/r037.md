# r037 — lgbm_xmas0

**Author:** researcher (session researcher-20260912-1)

**Hypothesis:** H031 — the store is closed on Christmas Day (sales are 0 on every 25 December
in the snapshot, 2012-2015, against ~4,000 units on a normal December day), but the baseline's
40 weekly training origins cover only ~280 days, so no training window ever contains a
Christmas and the model forecasts a normal day for 25 Dec 2015, which sits at h5 of fold 3
and h19 of fold 2.

**What changed:** `src/features.py` `Panel.__init__` adds a calendar column `christmas`
(event_name_1 == "Christmas"; calendar-known, horizon-varying, leak-free by construction).
`src/model.py`: `LGBMBaseline` gained a `FEATURES` list, an `extra_config()` hook and a
`postprocess(predict, preds)` hook (identity for the baseline; its config hash 900159c6f3dd
is unchanged, verified). New class `LGBMChristmasZero` (`lgbm_xmas0`) overrides
`postprocess` to set the forecast to 0 where `christmas == 1`. No model input changed.

**Result vs current best (r033, 0.810828):** WRMSSE 0.805456 (spread 0.000128), paired gain
+0.005372 against a threshold of 0.000345. Fold 2 0.838722 -> 0.816463, fold 3 0.854955 ->
0.834237; folds 1 and 4-8 identical to the parent to six decimals (no Christmas in their
windows). WAPE h1-7 0.7733 -> 0.7559, h15-28 0.7935 -> 0.7848. Bias +0.0087 -> +0.0007.
Keep rule: all three conditions PASS. Detail in runs/detail/r037.json.

**Verdict:** keep. Committed on exp/r037.
