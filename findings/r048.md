# r048 — lgbm_xmas0_r3_syoy

**Author:** researcher (session researcher-20260912-1)

**Hypothesis:** H037 — store-level year-ago seasonal shape on top of r044: for each horizon
date d, mean(store total over d-364 +/- 3 days) / mean(store total over the 28 days before
origin-364). A horizon-varying, series-constant level-shift signal from an aggregate that is
far less noisy than the series-level year-ago features that hurt February in r039/r040.

**What changed:** `src/features.py`: `Panel` gains `store_total`; new
`store_yoy_shape(panel, origin_pos, horizon)` (reads >= 334 days before the origin,
asserted; verified against a manual computation and invariant to zeroing the future), added
to `build_frame`. At the fold-1 origin the shape runs 1.00 (h1) -> 0.92 (h4) -> 0.85 (h7)
-> 1.04 (h14). `src/model.py`: new class `LGBMXmasStoreYoy` (`lgbm_xmas0_r3_syoy`) = r044
FEATURES + `store_yoy_shape`. Config hashes of lgbm_xmas0 / lgbm_xmas0_r3 unchanged.

**Result vs current best (r044, 0.804061):** WRMSSE 0.803104 (spread 0.000799), paired gain
+0.000957 against a threshold of 0.001598. Per fold (r044 -> r048): 1 0.8297 -> 0.8294
(-0.0003), 2 0.8142 -> 0.8136 (-0.0006), 3 0.8351 -> 0.8314 (-0.0036), 4 0.7854 -> 0.7855
(+0.0001), 5 0.7942 -> 0.7938 (-0.0004), 6 0.8016 -> 0.8005 (-0.0011), 7 0.7905 -> 0.7887
(-0.0019), 8 0.7819 -> 0.7820 (+0.0001). WAPE 0.7717 -> 0.7698; bias +0.0007 -> -0.0060;
fold-1 bias +0.054 -> +0.047. Keep rule: 1 FAIL, 2 PASS (8/8), 3 PASS. Detail in
runs/detail/r048.json.

The closest miss of the session after r043: six folds better, none worse, but the gain is
60% of the threshold. Unlike the series-level year-ago features it does not hurt February,
which confirms that r039/r040's February loss was series-level noise. It still leaves fold 1
almost untouched: the fold-1 over-forecast is not explained by last year's store-level shape
either.

**Verdict:** discard. Change reverted.
