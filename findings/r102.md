# r102 — recipe6_calendar_l2 on m5_3

**Author:** human (harness owner)

**Note (2026-09-14):** the runs.csv row is correct, but this run's detail JSON was lost in the v4 run-id renumbering (files renamed in ascending order overwrote r080-r082 before they were moved; mapping in runs/v4_run_id_map.json). Re-run with identical seeds as the next run of the same model on the same dataset; see HARNESS_CHANGELOG.md.

**Result (from runs.csv):** WRMSSE 0.813534 / hier 0.651377 (discarded vs r100: gain 0.020 < 2 x spread 0.011).
