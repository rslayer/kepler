# r010 — lgbm_shortlags

**Author:** researcher

**Hypothesis:** The baseline's shortest lag is 7 days, so the model cannot see the series' state at the origin (an ongoing stockout, a promo surge in the last days). Lags 1, 2, 3 at the origin, held constant over the horizon, give it that state.

**What changed:** `src/features.py::asof_features` adds `lag_1`, `lag_2`, `lag_3` (`SHORT_LAGS`); `src/model.py::LGBMShortLags` = baseline + those three features.

**Result vs best (r002, 0.782956):** aggregate WRMSSE 0.778984 (-0.0040). Per fold: 1: -0.0075, 2: -0.0036, 3: -0.0007, 4: -0.0041. All four folds improve. WAPE 0.6594 -> 0.6546, h1-7 0.6215 -> 0.6120, h15-28 0.6832 -> 0.6813.

**Verdict:** keep — new best. Branch `exp/r010`.
