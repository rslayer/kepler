# r045 — lgbm_xmas0_r3_mw

**Author:** researcher (session researcher-20260912-1)

**Hypothesis:** H024 — tempered metric-aligned sample weights on top of r044: per series and
per training origin, sqrt(dollar volume over the 28 days before that origin / RMSSE scale),
normalised to mean 1 and clipped to [0.2, 5]. v0 r016 used the untempered ratio (13%
zero-weight rows, 14x tails) and its verdict named this variant.

**What changed:** `src/features.py`: new `metric_weights(panel, origin_pos)` (reads only
columns < origin_pos, asserted; verified invariant to zeroing the future; 11% of series at
the lower clip, 0.03% at the upper, mean 0.92, p90 1.55). `src/model.py`:
`LGBMBaseline._fit_predict` passes `sample_weight` when the training frame carries that
column (absent for every existing model; lgbm_xmas0 and lgbm_xmas0_r3 config hashes verified
unchanged). New class `LGBMXmasMetricWeights` (`lgbm_xmas0_r3_mw`) builds the training set
with the weights attached.

**Result vs current best (r044, 0.804061):** WRMSSE 0.804058 (spread 0.001220), paired gain
+0.000003: neutral. Per fold (r044 -> r045): 1 0.8297 -> 0.8315 (+0.0019), 2 0.8142 ->
0.8147 (+0.0006), 3 0.8351 -> 0.8315 (-0.0036), 4 0.7854 -> 0.7852 (-0.0002), 5 0.7942 ->
0.7942 (0.0000), 6 0.8016 -> 0.8024 (+0.0008), 7 0.7905 -> 0.7904 (-0.0001), 8 0.7819 ->
0.7826 (+0.0006). WAPE h15-28 0.7840 -> 0.7803; bias +0.0007 -> -0.0063. Keep rule: 1 FAIL,
2 PASS (8/8), 3 PASS. Detail in runs/detail/r045.json.

Tempering removes r016's harm but leaves no gain: re-weighting rows toward the high-dollar,
smooth series does not change what the trees learn for them, because those series already
dominate the squared-error loss by their scale. The metric-weight lever is exhausted
(untempered hurts, tempered is neutral).

**Verdict:** discard. Change reverted.
