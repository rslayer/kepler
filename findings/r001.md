# r001 — seasonal_naive

**Author:** human (Phase 1 baseline)

**Hypothesis:** Seasonal naive (repeat last observed week) as the floor every model must clear.

**What changed:** nothing; this is a baseline run of `src/model.py::seasonal_naive`.

**Result:** WRMSSE 1.016667 on 4 folds. See `runs/detail/r001.json` for per-fold and per-series breakdown.

**Verdict:** keep — reference point.
