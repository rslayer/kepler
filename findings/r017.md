# r017 — lgbm_sl_tweedie

**Author:** researcher

**Hypothesis:** Retry Tweedie (variance power 1.2) on top of the kept short lags (r010).

**What changed:** `src/model.py::LGBMShortLagsTweedie` = r010 with `objective="tweedie"`, `tweedie_variance_power=1.2`.

**Result vs best (r010, 0.778984):** aggregate WRMSSE 0.784364 (+0.0054). Per fold: 1: +0.0006, 2: +0.0149, 3: **-0.0051**, 4: +0.0111. WAPE better (0.6489) but bias -0.050; the log-link model under-forecasts.

**Verdict:** discard as a standalone objective. Note it is the only change so far that clearly helps fold 3, which makes it an ensemble candidate.
