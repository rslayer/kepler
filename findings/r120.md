# r120 — recipe6_per_store on m5_all: per-store vs global recipe, bagged (v5 Part C confirmation tier)

**Author:** human (harness owner) · session v5-partc-20260914 · hypothesis H156

**Result:** status ok; bagged=True; WRMSSE 0.852129 (single-seed spread 0.000810); WRMSSE_hier **0.702771** (spread 0.001934); bias −1.99%; 6673.5 s (JOBS=3; 10 stores × 4 weeks × 3 seeds × 8 folds = 960 fits).

**Keep rule vs r118 (global recipe, bagged, wrmsse_hier):** verdict **discarded**. Paired gain −0.0075 vs threshold 0.0317; fold 2 regresses +0.047 (tol 0.016); bias guardrail PASS (0.020 vs 0.0086 + 0.02).

**What happened:** on all ten stores the per-store recipe is slightly worse than the global one (0.703 vs 0.695): better on folds 4, 6, 8 (−0.009 to −0.014), worse on folds 2, 3, 7 (+0.019 to +0.047), and more negatively biased on the winter folds (−4% to −5% in Jan–Feb). The m5_3 gain (r119: 0.638 vs 0.649, 7/8 folds) does not transfer: with ten stores the global model's cross-store pooling is worth more than store-specific fits, especially for the low-volume stores and after the holidays, and the per-store seed-spread reduction (0.0019 vs 0.0159) does not offset the loss in level. Both tiers agree on one thing: per-store fits are far less seed-sensitive than the global recipe.

**Verdict:** discard. Ingredient 7 as a pure replacement of the global model is not the accuracy lever on m5_all; a store-level *component* (blend with the global model, or store-specific residual correction) is the untried variant.
