# r067 — recipe2_tweedie

**Author:** human (harness owner; SPEC_v4 Part C recipe chain)

**Hypothesis (H102):** ingredient 2 Tweedie 1.1 + level_ratio — timed out at lr 0.02, one change on top of its parent r066.

**What changed:** `src/model.py::recipe2_tweedie` (and the feature builders it consumes in `src/features.py`).

**Result:** status=timeout; WRMSSE n/a (spread n/a) vs parent 0.806105; vs champion r037 0.805456. Verdict=discarded. timeout; rerun as r069

**Verdict:** discard.
