# r008 — lgbm_origins80

**Author:** researcher

**Hypothesis:** With 40 weekly training origins the training targets span only ~280 days, so fold 1 (origin 2015-12-07, window includes Christmas) never sees a Christmas in training, and no fold sees a full year of seasonality. 80 origins (~560 days) should fix the December fold and help long horizons generally.

**What changed:** `src/model.py::LGBMOrigins80` = baseline with `N_TRAIN_ORIGINS = 80`. Features and params unchanged.

**Result vs best (r002, 0.782956):** aggregate WRMSSE 0.772707 (-0.0102, the largest gain so far). Per fold: 1: **-0.0304**, 2: -0.0146, 3: +0.0018, 4: +0.0022. Fold 1 h15-28 WAPE 0.797 -> 0.776. Bias flipped to +0.015.

**Verdict:** investigate — huge win on the two winter folds, small loss on the two late-winter folds. Fails the all-folds rule. Older data appears to dilute recent behaviour for folds 3/4; next: fewer extra origins (60) and, separately, recency-weighted training rows.
