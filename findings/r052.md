# r052 — lgbm_xmas0_r3_dept

**Author:** researcher (session researcher-20260912-1)

**Hypothesis:** H030 — r044 fit as seven per-department models instead of one model over
all 3,049 series (same features, params, origins and seeds; the frames are partitioned by
the item_id prefix). Departments behave differently (the Thanksgiving ratios in r050 range
from 0.33 to 1.30), so a pooled model might be averaging across them.

**What changed:** `src/model.py`: new class `LGBMXmasPerDept` (`lgbm_xmas0_r3_dept`) whose
`_fit_predict` splits train and predict by department and calls the parent fit on each
partition. Nothing in `src/features.py` changed; lgbm_xmas0_r3 config hash unchanged.

**Result vs current best (r044, 0.804061):** WRMSSE 0.806045 (spread 0.000598), paired gain
-0.001984 (worse). Per fold (r044 -> r052): 1 0.8297 -> 0.8370 (+0.0074), 2 0.8142 -> 0.8217
(+0.0075), 3 0.8351 -> 0.8324 (-0.0027), 4 0.7854 -> 0.7884 (+0.0030), 5 0.7942 -> 0.7979
(+0.0037), 6 0.8016 -> 0.8021 (+0.0006), 7 0.7905 -> 0.7854 (-0.0051), 8 0.7819 -> 0.7834
(+0.0015). WAPE 0.7717 -> 0.7700 (better) but fold-4 bias -0.049. Keep rule: 1 FAIL, 2 FAIL
(5/8 folds regress, worst fold 2 +0.007512 vs tolerance 0.001537), 3 PASS. Runs 1.7x slower.
Detail in runs/detail/r052.json.

Pooling across departments helps WRMSSE: the pooled model with item_id as a categorical
already separates departments where it matters and borrows strength where they are alike.
Partitioning also makes the single-department trees noisier on the small departments
(HOBBIES_2 has 149 series). Architecture changes stay last in the preference order.

**Verdict:** discard. Change reverted.
