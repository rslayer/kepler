# r063 — lgbm_xmas0_tgd_r3_dmom

**Author:** researcher (session researcher-20260913-2)

**Hypothesis:** H044 — department-level short-term state at the origin, the "same state at
department level" sibling of roll_mean_3 named in PRIORS: `grp_dept_id_r3_28` = department
mean over the 3 days before origin / department mean over the 28 days before origin. A
denoised momentum signal shared by a department's series, on top of r061.

**What changed:** `src/features.py`: `asof_features` also emits `grp_<attr>_r3_28` for every
series attribute with 2-50 groups (dept_id, cat_id), from the cached `group_totals`; reads
origin_pos-28 .. origin_pos-1 (asserted). Not in FEATURE_COLUMNS. `src/model.py`: new class
`LGBMDeptMomentum` (`lgbm_xmas0_tgd_r3_dmom`) = r061 features + `grp_dept_id_r3_28`.

**Result vs current best (r061, 0.803303):** WRMSSE 0.802851 (spread 0.000537), paired gain
+0.000452 against a threshold of 0.001315. Per fold (r061 -> r063): 1 0.8236 -> 0.8233
(-0.0003), 2 0.8142 -> 0.8130 (-0.0011), 3 0.8351 -> 0.8340 (-0.0011), 4 0.7854 -> 0.7848
(-0.0007), 5 0.7942 -> 0.7951 (+0.0009), 6 0.8016 -> 0.8015 (-0.0001), 7 0.7905 -> 0.7897
(-0.0009), 8 0.7819 -> 0.7814 (-0.0005). Bias unchanged (-0.0045); WAPE unchanged. Keep
rule: 1 FAIL, 2 PASS (8/8), 3 PASS. Detail in runs/detail/r063.json.

Seven of eight folds improve and the seed spread falls slightly (0.00066 -> 0.00054), but the
gain is a third of the threshold. Department momentum carries a little information the series
features do not; not enough to be distinguishable from seed noise.

**Verdict:** discard. Change reverted.
