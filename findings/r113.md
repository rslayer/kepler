# r113 — recipe_calib on m5_3: per store × department calibration on the validation window

**Author:** human (harness owner) · session lever-20260914-1 · hypothesis H145

**Result:** status ok; WRMSSE 0.815586 (spread 0.000416); WRMSSE_hier 0.672521 (spread 0.002764); bias −0.001974; 6290.5 s (JOBS=3).

**Keep rule vs r107 (wrmsse_hier):** verdict **discarded**. Paired gain −0.0243 vs threshold 0.0055; folds regressing beyond tolerance: 1, 2, 3, 5, 6, 7, 8 (fold 2 +0.065); bias guardrail PASS.

**What happened:** the factor (sum actual + 200) / (sum predicted + 200) per store × department over the 28 days before the origin, clipped to [0.8, 1.25], did shrink the overall bias (−0.37% → −0.20%) and pulled most folds' bias toward zero, but the hierarchical score worsened on 7/8 folds (0.648 → 0.673). The model's errors on the validation window are not the errors it will make on the forecast window: after the holidays (folds 2, 3) the window still contains December, so the factor over-corrects upward; on calm folds the correction is small and noisy. Bias at the total is not a stable per-group multiplicative offset on this data.

**Verdict:** discard. End of the bias chain (r109, r110, r112, r113): none of ratio target, momentum features, weighted ratio target, or validation-window calibration beats the bagged recipe on the screen. The screen's own verdicts are what SPEC_v5 Part A now addresses (seed-averaged forecasts before scoring).
