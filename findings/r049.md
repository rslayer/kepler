# r049 — lgbm_xmas0_r3_tg

**Author:** researcher (session researcher-20260912-1)

**Hypothesis:** H038 — Thanksgiving-weekend override on top of r044. A single-seed fold-1
diagnostic of r044 showed the store total over-forecast 1.58x on Thanksgiving Day, 1.53x on
Black Friday, 1.35x Saturday, 1.19x Sunday (this grocery-heavy store dips for four days); no
40-origin training window contains a Thanksgiving and Black Friday is not a calendar event.
Same mechanism as the kept Christmas-zero (H031), with the multiplier taken from history.

**What changed:** `src/features.py`: `Panel` gains `store_total` and `thanksgiving_pos`; new
`thanksgiving_multipliers(panel, origin_pos, horizon)` = 1.0 except on Thanksgiving Day and
the three days after, where it is the mean over prior years (day strictly before the origin,
asserted; verified invariant to zeroing the future) of store_total[T+k] / mean of the four
same-weekday totals before it. Prior-year ratios Thu..Sun: 2011 0.68/0.92/0.70/0.76, 2012
0.69/0.64/0.62/0.77, 2013 0.73/0.64/0.64/0.86, 2014 0.70/0.81/0.78/0.81; fold-1 multipliers
0.70/0.75/0.69/0.80 at h4-h7; every other fold origin gets none. Added to `build_frame` as
`tg_mult`. `src/model.py`: new class `LGBMXmasThanksgiving` (`lgbm_xmas0_r3_tg`), whose
`postprocess` multiplies the Christmas-zeroed forecast by `tg_mult`. No model input changed;
lgbm_xmas0 / lgbm_xmas0_r3 config hashes unchanged.

**Result vs current best (r044, 0.804061):** WRMSSE 0.803408 (spread 0.000672), paired gain
+0.000653 against a threshold of 0.001344. Fold 1 0.829669 -> 0.824444 (-0.0052), fold-1
bias +0.054 -> +0.012, WAPE h1-7 0.7534 -> 0.7460; folds 2-8 identical to r044 to six
decimals. Keep rule: 1 FAIL, 2 PASS (8/8; worst delta 0.000000), 3 PASS. Detail in
runs/detail/r049.json.

The mechanism is right (the bias is gone) but the WRMSSE gain is a third of what the threshold
needs: a uniform store-level multiplier corrects the total while the per-series squared
errors need the dip where it actually happens (h1-7 over-forecast by department: FOODS_2
+0.22, HOUSEHOLD_2 +0.21, HOBBIES_1 +0.15, HOUSEHOLD_1 +0.13, FOODS_3 +0.07, FOODS_1 +0.03).
Department-level ratios (H039) are the one-variable refinement.

**Verdict:** discard. Change reverted.
