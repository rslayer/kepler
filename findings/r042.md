# r042 — lgbm_xmas0_ens_tw

**Author:** researcher (session researcher-20260912-1)

**Hypothesis:** H015 under the v1 harness — equal-weight average of the r037 L2 member and a
Tweedie (variance power 1.2) member with otherwise identical params, features and origins;
Christmas-zero applied to the blend. The priors name a Tweedie member as the second lever
(v0 r021/r022: Tweedie is the one change that helped the late-winter fold).

**What changed:** `src/model.py`: `LGBMBaseline._fit_predict` gained an optional `params`
argument (default = `self.PARAMS`; config hashes of lgbm_baseline and lgbm_xmas0 verified
unchanged). New class `LGBMXmasTweedieBlend` (`lgbm_xmas0_ens_tw`): fits both members per
seed and returns 0.5 x L2 + 0.5 x Tweedie.

**Result vs current best (r037, 0.805456):** WRMSSE 0.805609 (spread 0.000606), paired gain
-0.000153 (neutral). WAPE improved in every bucket (0.7732 -> 0.7654; h15-28 0.7848 ->
0.7774) but bias moved +0.0007 -> -0.0115. Per fold (r037 -> r042): 1 0.8302 -> 0.8271
(-0.0031), 2 0.8165 -> 0.8165 (0.0000), 3 0.8342 -> 0.8423 (+0.0080), 4 0.7889 -> 0.7907
(+0.0018), 5 0.7955 -> 0.7947 (-0.0007), 6 0.8028 -> 0.7994 (-0.0035), 7 0.7896 -> 0.7886
(-0.0010), 8 0.7859 -> 0.7855 (-0.0004). Keep rule: 1 FAIL, 2 FAIL (fold 3 +0.008033 vs
tolerance 0.006087), 3 PASS. Detail in runs/detail/r042.json.

The v0 lesson holds on v1: Tweedie improves WAPE, not WRMSSE, and pulls the blend under
(fold 3 bias -0.018, fold 4 -0.033, fold 6 -0.034). The blend's fold pattern (gains on
folds 1 and 6-8, loss on fold 3) is the mirror image of the year-ago features (r039: gains
on 1-3, losses on 6-8), but two changes cannot be combined in one run and neither passes on
its own. A Tweedie member needs its level bias fixed first (H027) before it can pass the
no-fold-regress rule.

**Verdict:** discard. Change reverted.
