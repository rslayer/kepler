# r050 — lgbm_xmas0_r3_tgd

**Author:** researcher (session researcher-20260912-1)

**Hypothesis:** H039 — the Thanksgiving-weekend override of r049 with department-level
prior-year ratios instead of one store-level ratio. r049 removed fold 1's bias but gained a
third of the threshold; the fold-1 diagnostic showed the dip differs by department.

**What changed:** `src/features.py`: `Panel` gains `dept_id`, `dept_index`, `dept_total`
and `thanksgiving_pos`; `_thanksgiving_ratios` (the r049 rule applied to any total series)
and `thanksgiving_multipliers_dept` (series x horizon, per department; reads only positions
< origin_pos, asserted; verified invariant to zeroing the future), added to `build_frame` as
`tg_mult_dept`. Fold-1 multipliers h4-h7: FOODS_1 0.57/1.12/0.87/0.90, FOODS_2
0.34/0.48/0.56/0.80, FOODS_3 0.93/0.77/0.68/0.76, HOBBIES_1 0.33/0.69/0.67/0.93, HOBBIES_2
0.79/1.30/0.96/0.98, HOUSEHOLD_1 0.56/0.72/0.73/0.83, HOUSEHOLD_2 0.55/0.95/0.70/0.84; no
other fold origin gets any. `src/model.py`: new class `LGBMXmasThanksgivingDept`
(`lgbm_xmas0_r3_tgd`) multiplies the Christmas-zeroed forecast by `tg_mult_dept`. No model
input changed; lgbm_xmas0 / lgbm_xmas0_r3 config hashes unchanged.

**Result vs current best (r044, 0.804061):** WRMSSE 0.803303 (spread 0.000658), paired gain
+0.000758 against a threshold of 0.001316. Fold 1 0.829669 -> 0.823604 (-0.0061; r049 got
-0.0052), fold-1 bias +0.054 -> +0.012, WAPE h1-7 0.7534 -> 0.7453; folds 2-8 identical to
r044. Keep rule: 1 FAIL, 2 PASS (8/8), 3 PASS. Detail in runs/detail/r050.json.

Department resolution adds only 0.0009 on fold 1 over the store-level rule. Both runs say the
same thing: the Thanksgiving-weekend dip is real, recurring (2011-2014) and worth ~0.006 of
fold-1 WRMSSE, which after dividing by eight folds is ~0.0008 of aggregate, and the harness
threshold (twice the seed spread of the whole run, ~0.0013) is set by seed noise on the other
seven folds that a postprocess cannot reduce. Any single-fold, single-week fix is below the
bar by construction; it would have to travel with a broader change.

**Verdict:** discard. Change reverted.
