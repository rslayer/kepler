# r043 — lgbm_xmas0_sl

**Author:** researcher (session researcher-20260912-1)

**Hypothesis:** H007 under the v1 harness — lags 1, 2, 3 at the origin (series state) on top
of r037. v0 r010 helped every fold at seed 42; the adversary found 72% of the gain in one
series and reversals at other seeds, so it was rated inconclusive.

**What changed:** `src/features.py`: `SHORT_LAGS = (1, 2, 3)`; `asof_features` now also
emits `lag_1`, `lag_2`, `lag_3` (as-of-origin, reads origin_pos - k; alignment verified
against the raw panel; not in FEATURE_COLUMNS, so lgbm_baseline/lgbm_xmas0 config hashes
are unchanged, verified). `src/model.py`: new class `LGBMXmasShortLags` (`lgbm_xmas0_sl`)
= r037 FEATURES + the three lags.

**Result vs current best (r037, 0.805456):** WRMSSE 0.803043 (spread 0.001235), paired gain
+0.002413 against a threshold of 0.002470 (2 x the child's own spread): FAIL by 0.000057.
Every fold improved (r037 -> r043): 1 0.8302 -> 0.8272 (-0.0030), 2 0.8165 -> 0.8130
(-0.0035), 3 0.8342 -> 0.8331 (-0.0011), 4 0.7889 -> 0.7856 (-0.0033), 5 0.7955 -> 0.7943
(-0.0012), 6 0.8028 -> 0.8016 (-0.0012), 7 0.7896 -> 0.7884 (-0.0013), 8 0.7859 -> 0.7813
(-0.0046). Bias +0.0007 -> 0.0000; WAPE 0.7732 -> 0.7724. Keep rule: 1 FAIL, 2 PASS (8/8),
3 PASS. Detail in runs/detail/r043.json.

The gain is broad (all eight folds, both halves of the horizon) rather than one series, which
answers the v0 adversary's objection; what fails is the threshold, and the threshold is set
by the child's seed spread, which the three noisy daily lags raised from 0.0001 to 0.0012.
A single smoothed series-state feature (mean of the last three days, H033) is the one-variable
follow-on: same information, less seed noise. Not a re-run of H007.

**Verdict:** discard. Change reverted.
