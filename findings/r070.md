# r070 — lgbm_xmas0_tgd_r3_o80hl120

**Author:** researcher (session researcher-20260913-2)

**Hypothesis:** H023 — 80 weekly training origins with a short recency half-life, on top of
r061. Every long-history run gains on the holiday folds (last year's Nov-Jan in training) and
loses fold 7/8 (r038, r065-r067); r069 showed fold 7 is the fold most dependent on an up-to-date
per-item level. Down-weighting old rows should keep last year's holiday shapes available while
the recent level dominates the item_id and level splits.

**What changed:** `src/model.py`: new class `LGBMO80HalfLife` (`lgbm_xmas0_tgd_r3_o80hl120`),
subclass of `LGBMTgdRoll3` with `N_TRAIN_ORIGINS = 80` and `_fit_predict` passing sample weight
0.5 ** (age / 120 days) per training origin (age = 7 x origin index from the newest; frames are
stacked newest first in equal blocks, asserted). Features, params and postprocess identical to
r061. The ledger row defines H023 as 80 origins with a 90-180 day half-life, so the two move
together as one hypothesis; 80 origins alone is H005 (discarded, r038).

**Result vs current best (r061, 0.803303):** WRMSSE 0.798186 (spread 0.001385), paired gain
+0.005117 against a threshold of 0.002770. Per fold (r061 -> r070): 1 0.8236 -> 0.8221
(-0.0015), 2 0.8142 -> 0.8038 (-0.0104), 3 0.8351 -> 0.8195 (-0.0156), 4 0.7854 -> 0.7767
(-0.0088), 5 0.7942 -> 0.7906 (-0.0036), 6 0.8016 -> 0.7998 (-0.0018), 7 0.7905 -> 0.7898
(-0.0008), 8 0.7819 -> 0.7833 (+0.0013, tol 0.0029). WAPE 0.7699 -> 0.7614 (every bucket
better: h1-7 0.7453 -> 0.7360, h15-28 0.7840 -> 0.7755); bias -0.0045 -> -0.0090. Keep rule:
1 PASS, 2 PASS (8/8), 3 PASS. Detail in runs/detail/r070.json. 2.3x slower (667 s).

The largest kept gain on the v1/v3 harness, and the first long-history variant that does not
lose February: seven folds improve, fold 7 turns from +0.014 (r066/r067) to -0.0008, and fold 8
is within its tolerance. Caveats for review: the seed spread doubles (0.00066 -> 0.00139), which
raises the bar for every later child; bias moves down by 0.0045 (the Feb folds 6/8 carry
-0.025/-0.007); and fold 3 still gains less here (-0.016) than with unweighted history
(-0.021/-0.022), so the half-life trades some holiday gain for February safety.

**Verdict:** keep. Committed on exp/r070.
