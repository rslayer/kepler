# r003 — lgbm_baseline

**Author:** human (Phase 1 baseline)

**Hypothesis:** Determinism check: exact repeat of r002 with the same seed. Metrics must match r002 to four decimals.

**What changed:** nothing; this is a baseline run of `src/model.py::lgbm_baseline`.

**Result:** WRMSSE 0.782956 on 4 folds. See `runs/detail/r003.json` for per-fold and per-series breakdown.

**Verdict:** keep — reference point.
