# r119 — recipe6_per_store on m5_3: one recipe chain per store vs the global recipe (v5 Part C)

**Author:** human (harness owner) · session v5-partc-20260914 · hypothesis H155

**Result:** status ok; bagged=True; WRMSSE 0.812959 (single-seed spread 0.000369); WRMSSE_hier **0.637942** (spread **0.001070**); bias −0.76%; 1995.9 s (JOBS=3; 3 stores × 4 weeks × 3 seeds × 8 folds = 288 fits, same wall clock as the global 96).

**Keep rule vs r115 (global recipe, bagged, wrmsse_hier):** verdict **discarded** on condition 1 only. Paired gain +0.010958 vs threshold 0.022241 = 2 × max(parent spread 0.011121, child spread 0.001070); no fold regresses (8/8; fold 2 +0.025 within its 0.038 tolerance); bias guardrail PASS (0.0076 vs 0.0045 + 0.02).

**What happened:** per-store models beat the global recipe on 7 of 8 folds (−0.005 to −0.026) and lose only the post-holiday fold 2 (+0.025). The per-store aggregate seed spread is a tenth of the global recipe's (0.0011 vs 0.0111): splitting by store removes the cross-store seed interaction that made the global recipe noisy. The verdict is discarded because condition 1 sets the bar from the *parent's* spread; the harness rule was not changed (SPEC_v5: do not loosen the keep rule). Against the bagged baseline r114 the per-store recipe still loses the four calm late-winter folds (+0.016 to +0.032), less than the global recipe did (+0.022 to +0.058).

**Verdict:** discard by the rule; strongest per-run result on the screen so far (best hier of any m5_3 run: 0.638 vs 0.645 momentum, 0.648 bag3). Note for the human: a child this much less noisy than its parent cannot clear a bar set by the parent's noise; the m5_all run (H156) is the confirmation that matters.
