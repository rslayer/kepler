# r060 — lgbm_xmas0_tgd

**Author:** researcher (session researcher-20260913-2)

**Hypothesis:** H041 — the department-level Thanksgiving-weekend override (the r050
mechanism) retested as its own single-variable run on the champion r037. Fold 1 (Thanksgiving
week) is the worst fold of r037 (WRMSSE 0.8302, bias +0.053, h1-7 WAPE 0.870). On r044 the
override removed the bias and gained ~0.0008 aggregate but missed a threshold of ~0.0013 set
by r044's seed spread (0.0006). The v3 champion r037 has no roll_mean_3 and a seed spread of
0.000128, so this is the first base on which a deterministic postprocess is judged against
its own noise rather than a feature's. H041's stated condition ("only if a future base changes
fold 1's shape") is not what changed; the base itself changed (exp/r044 cannot merge), and I
record that openly.

**What changed:** `src/features.py`: new generic helpers `event_positions` (exog columns of a
named calendar event), `group_totals` (per-attribute summed target, cached on the panel) and
`event_window_multipliers(panel, origin_pos, horizon, event, group_attr, days=4,
baseline_weeks=4)`: 1.0 except on the event day and the three following days inside the
horizon, where it is the mean over prior occurrences of group_total[T'+k] / mean of the four
same-weekday totals before it; only occurrences with T'+k < origin_pos are used (asserted).
`build_frame` is untouched. `src/model.py`: new class `LGBMXmasThanksgivingDept`
(`lgbm_xmas0_tgd`), subclass of `LGBMChristmasZero`, computes the multipliers for
("Thanksgiving", "dept_id") at the fold origin and multiplies the Christmas-zeroed forecast.
No model input changed; lgbm_xmas0 config hash unchanged (b759aafd2f04).

**Result vs current best (r037, 0.805456):** WRMSSE 0.804787 (spread 0.000139), paired gain
+0.000669 against a threshold of 0.000278. Fold 1 0.830167 -> 0.824813 (-0.0054), fold-1 bias
+0.053 -> +0.011, fold-1 WAPE 0.836 -> 0.821; folds 2-8 identical to r037 to six decimals.
Aggregate bias +0.0007 -> -0.0046; WAPE h1-7 0.7559 -> 0.7478. Keep rule: 1 PASS, 2 PASS (8/8,
worst delta 0.000000), 3 PASS. Detail in runs/detail/r060.json.

Same fold-1 effect as r050 on r044 (-0.0061 there), now above the bar because the parent is
quiet. Like Christmas-zero (r037), the gain is confined to the only fold window that contains
the dates and is recurring history (2011-2014 ratios), not a fitted parameter. Caveat for the
adversary: it is one fold's gain, the item-5 concentration pattern the human overruled for
r037.

**Verdict:** keep. Committed on exp/r060.
