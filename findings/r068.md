# r068 — lgbm_xmas0_tgd_r3_lr025

**Author:** researcher (session researcher-20260913-2)

**Hypothesis:** H049 — half the boosting step at the same total shrinkage budget
(learning_rate 0.025 x 600 trees instead of 0.05 x 300) on top of r061. Smaller steps usually
give a smoother ensemble; num_leaves 31 (r051) showed the December folds reward smoothness.

**What changed:** `src/model.py`: new class `LGBMSmallSteps` (`lgbm_xmas0_tgd_r3_lr025`),
subclass of `LGBMTgdRoll3` with PARAMS learning_rate 0.025 and n_estimators 600. The two
parameters move together as one change (step size at a fixed lr x trees budget).

**Result vs current best (r061, 0.803303):** WRMSSE 0.802830 (spread 0.000733), paired gain
+0.000473 against a threshold of 0.001466. Per fold (r061 -> r068): 1 0.8236 -> 0.8240
(+0.0004), 2 0.8142 -> 0.8136 (-0.0006), 3 0.8351 -> 0.8325 (-0.0026), 4 0.7854 -> 0.7861
(+0.0007), 5 0.7942 -> 0.7947 (+0.0005), 6 0.8016 -> 0.8011 (-0.0005), 7 0.7905 -> 0.7889
(-0.0016), 8 0.7819 -> 0.7818 (-0.0001). Bias and WAPE unchanged. Keep rule: 1 FAIL, 2 PASS
(8/8), 3 PASS. Detail in runs/detail/r068.json. 2x slower (629 s).

Neutral: a third of the threshold at twice the cost; the seed spread did not fall (0.00066 ->
0.00073). The model is not step-size limited.

**Verdict:** discard. Change reverted.
