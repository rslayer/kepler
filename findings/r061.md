# r061 — lgbm_xmas0_tgd_r3

**Author:** researcher (session researcher-20260913-2)

**Hypothesis:** H042 — port H033 (roll_mean_3, the smoothed series state at the origin, kept
as r044 on the pre-v3 contract, branch unmergeable) onto the v3 chain, on top of r060. r043/r044
showed short-window series state improves 6-8 of 8 folds.

**What changed:** `src/features.py`: `SHORT_ROLL_WINDOWS = (3,)`; `asof_features` also emits
`roll_mean_3` (reads origin_pos-3 .. origin_pos-1, asserted; not in FEATURE_COLUMNS, so the
lgbm_xmas0 / lgbm_xmas0_tgd config hashes are unchanged). `src/model.py`: new class
`LGBMTgdRoll3` (`lgbm_xmas0_tgd_r3`) = r060 features + `roll_mean_3` (appended last, as in
r044). Nothing else changed.

**Result vs current best (r060, 0.804787):** WRMSSE 0.803303 (spread 0.000658), paired gain
+0.001484 against a threshold of 0.001316. Per fold (r060 -> r061): 1 0.8248 -> 0.8236
(-0.0012), 2 0.8165 -> 0.8142 (-0.0023), 3 0.8342 -> 0.8351 (+0.0008), 4 0.7889 -> 0.7854
(-0.0035), 5 0.7955 -> 0.7942 (-0.0013), 6 0.8028 -> 0.8016 (-0.0013), 7 0.7896 -> 0.7905
(+0.0009), 8 0.7859 -> 0.7819 (-0.0040). Bias -0.0046 -> -0.0045; WAPE 0.7714 -> 0.7699.
Keep rule: 1 PASS, 2 PASS (8/8; worst fold 7 +0.00088 vs tol 0.0019), 3 PASS. Detail in
runs/detail/r061.json.

The numbers are identical to six decimals to r050 (r044 + the same override), so the port
reproduces r044 exactly under the v3 contract. The adversary's r054 caveat carries over: the
gain is seed-fragile in size (66% in the top 5% of series), and it raises the chain's seed
spread from 0.00014 to 0.00066, which doubles the bar for every later child.

**Verdict:** keep. Committed on exp/r061.
