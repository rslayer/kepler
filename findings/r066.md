# r066 — lgbm_xmas0_tgd_r3_yao

**Author:** researcher (session researcher-20260913-2)

**Hypothesis:** H047 — keep r061's 40 recent weekly training origins and add the 7 weekly
origins centred on the fold origin 364 days earlier (weeks -3..+3), so last year's same-season
window enters training without diluting the recent level. r065 (40 origins at 14-day spacing)
gained 0.0044 but lost February folds 6/7 with the bias moving down, which looked like level
dilution.

**What changed:** `src/model.py`: `LGBMBaseline.forecast` now builds its training matrix through
a `training_set` hook (baseline behaviour identical); new class `LGBMYearAgoOrigins`
(`lgbm_xmas0_tgd_r3_yao`) appends `build_frame(..., with_target=True)` for origins fold_origin -
364 + 7j, j = -3..3 (target windows asserted to close before the fold origin; no overlap with the
40 recent origins, whose earliest is fold_origin - 301).

**Result vs current best (r061, 0.803303):** WRMSSE 0.800484 (spread 0.000198), paired gain
+0.002819 against a threshold of 0.001315. Per fold (r061 -> r066): 1 0.8236 -> 0.8209
(-0.0027), 2 0.8142 -> 0.8068 (-0.0074), 3 0.8351 -> 0.8126 (-0.0224), 4 0.7854 -> 0.7760
(-0.0094), 5 0.7942 -> 0.7936 (-0.0005), 6 0.8016 -> 0.8012 (-0.0004), 7 0.7905 -> 0.8044
(+0.0139, tol 0.0054), 8 0.7819 -> 0.7883 (+0.0064). Bias -0.0045 -> -0.0031. Keep rule: 1
PASS, 2 FAIL (folds 7 and 8), 3 PASS. Detail in runs/detail/r066.json.

The level-dilution explanation of r065 is wrong, or incomplete: the recent origins are intact
here and folds 7/8 lose more, not less, while fold 6 is flat and bias barely moves. What folds
7 and 8 share is horizon days in March (fold 7 Feb 15-Mar 13, fold 8 Feb 29-Mar 27), and the
40 recent origins contain no March target rows at all, so the year-ago rows are the model's
only examples of month = 3 and a month split transfers March 2015 behaviour wholesale. The
holiday folds gain for the mirror reason (last year's December rows). The confound is the
`month` feature meeting a single year of that month.

**Verdict:** discard (investigate). Change reverted (including the hook, which was a no-op
refactor).
