# r022 — ens_mcs200_tweedie

**Author:** researcher

**Hypothesis:** r021's blend lost folds 2 and 4 because its L2 member (r010) was the weaker one there; r018 (min_child_samples 200) beat r010 on folds 1, 2, 4. Swapping the L2 member for r018 keeps Tweedie's fold-3 gain and adds r018's gains elsewhere.

**What changed:** `src/model.py::EnsembleMCS200Tweedie` = equal-weight average of `lgbm_sl_mcs200` (r018) and `lgbm_sl_tweedie` (r017). Single change vs r021: the L2 member.

**Result vs best (r010, 0.778984):** aggregate WRMSSE 0.774048 (-0.0049). Per fold: 1: -0.0053, 2: -0.0078, 3: -0.0058, 4: -0.0008. All four folds improve. WAPE 0.6468 (best of the session), bias -0.040. Runtime 45s.

**Verdict:** keep — new best. Branch `exp/r022`. Note the negative bias inherited from the Tweedie member is the obvious next lever.
