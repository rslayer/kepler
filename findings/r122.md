# r122 — recipe6_l2_slow on m5_3: restore the spec learning schedule (research, H158)

**Author:** researcher · session research-20260916-1 · hypothesis H158 · parent r115 (bagged recipe)

**Hypothesis:** the recipe's capacity was cut from the spec (lr 0.02 / 3000 trees) to lr 0.05 / 1500 for wall-clock in v4; restoring the slower learning schedule should lift accuracy and help close the leaderboard gap.

**What changed:** one variable — `recipe6_l2_slow` = recipe6_calendar_l2 with learning_rate 0.02, n_estimators cap 3000, early-stopped. Every feature, the direct per-week split, the L2 objective and the bagging are unchanged.

**Result:** status ok; bagged=True; WRMSSE_hier **0.646371** vs recipe r115 0.648900 (+0.0025); bias −0.40%; 3419 s (57 min, JOBS=3 — ~40% slower than the budget schedule).

**Keep rule vs r115 (v6.1, wrmsse_hier):** verdict **discarded**. Sign test 6/8 folds improve, p=0.1445 (not significant); condition 2 fails (fold 5 +0.009 > tol 0.006); median gain +0.004 passes. Per-fold: better on 6 (folds 1,3,4,6,7,8 by +0.003..+0.012), worse on folds 2 (−0.003) and 5 (−0.009).

**Verdict:** discard. The capacity restore is a small, broad, but non-significant improvement (~+0.0025 hier) at ~40% more fit time. Too small to matter against the 0.05 leaderboard gap (recipe yardstick 0.626 → rank-50 0.576), and it regresses two calm folds. Not worth the m5_all confirmation (≈3–4 h at this schedule for an expected sub-0.005 gain). The gap needs a structural lever (hierarchical reconciliation), not hyperparameter tuning. recipe6_l2_slow stays registered as a documented discarded experiment.
