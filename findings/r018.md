# r018 — lgbm_sl_mcs200

**Author:** researcher

**Hypothesis:** Intermittent counts are noisy; `min_child_samples=50` on 920k rows lets leaves fit noise. Raising it to 200 regularises and should lower variance across folds.

**What changed:** `src/model.py::LGBMShortLagsMCS200` = r010 with `min_child_samples=200`.

**Result vs best (r010, 0.778984):** aggregate WRMSSE 0.774148 (-0.0048). Per fold: 1: -0.0004, 2: -0.0157, 3: **+0.0005**, 4: -0.0037.

**Verdict:** investigate — misses the all-folds rule by 0.0005 on fold 3, which is inside fold noise. Strong candidate to combine with a fold-3-friendly change.
