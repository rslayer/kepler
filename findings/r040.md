# r040 — lgbm_xmas0_yoylvl

**Author:** researcher (session researcher-20260912-1)

**Hypothesis:** H032 — one horizon-aligned year-ago feature rescaled to the current level:
`yoy_roll_7_lvl` = mean(sales[d-364 +/- 3]) x roll_mean_28(origin) / roll_mean_28(origin-364).
r039's raw year-ago features helped the Nov-Jan folds and hurt the February folds; the idea
was that last year's shape is useful but last year's level is stale in February.

**What changed:** `src/features.py`: new `yearago_level_features(panel, origin_pos, horizon)`
(reads >= 334 days before the origin plus the 28 days before it; asserted; alignment and
NaN placement verified against the raw panel at three origins), called from `build_frame`.
`src/model.py`: new class `LGBMXmasYoyLevel` (`lgbm_xmas0_yoylvl`) = r037 FEATURES +
`yoy_roll_7_lvl`. NaN where last year's 28-day level is zero (9-25% of rows depending on
the origin).

**Result vs current best (r037, 0.805456):** WRMSSE 0.807781 (spread 0.001060), paired gain
-0.002325 (worse). Per fold (r037 -> r040): 1 0.8302 -> 0.8226 (-0.0075), 2 0.8165 -> 0.8122
(-0.0043), 3 0.8342 -> 0.8266 (-0.0076), 4 0.7889 -> 0.7865 (-0.0024), 5 0.7955 -> 0.7979
(+0.0025), 6 0.8028 -> 0.8106 (+0.0077), 7 0.7896 -> 0.8056 (+0.0160), 8 0.7859 -> 0.8002
(+0.0143). Keep rule: 1 FAIL, 2 FAIL (folds 5-8 regress, worst fold 7 +0.015960 vs tolerance
0.002326), 3 PASS. Detail in runs/detail/r040.json.

Rescaling did not rescue February; it halved the holiday-fold gains and doubled the February
losses versus r039. So the February loss is not a stale-level effect: last year's shape for
February/early March is itself a poor guide (fold 6-8 windows: Super Bowl, Valentine's,
Presidents' Day, leap day, all of which sit on different dates/weekdays 364 days earlier).
With r038 (80 origins), that is three runs in which information from a year ago gains on the
Nov-Jan folds and loses on the Feb folds. Year-ago features are exhausted; if revisited, the
Feb-fold loss needs a mechanism (event-alignment, not level) before another run.

**Verdict:** discard. Change reverted.
