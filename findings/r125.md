# r125 — recipe6_reconciled on m5_all: confirmation (research, H159)

**Author:** researcher · session research-20260916-1 · hypothesis H159 · parent r118 (bagged recipe m5_all)

**Result:** status ok; bagged=True; WRMSSE_hier **0.723202** vs recipe r118 0.695249 (−0.0280, WORSE); bias +0.0077 (recipe −0.0086); 6729 s.

**Keep rule vs r118 (v6.1):** verdict **discarded**. Sign test 2/6 folds improve (p=0.89); fold 5 regresses +0.088; median gain −0.006; mean gain −0.028.

**Per-level (reconciled − recipe):** EVERY level is worse — L1 +0.047, L2/L3 +0.041/+0.048, L4 +0.028, L5 +0.033, L6-L9 +0.026 to +0.035, even item levels +0.005 to +0.010. Per-fold: folds 3-4 help (+0.013, +0.054) but folds 5-7 blow up (−0.088, −0.100, −0.091). Bias flips from −0.9% to +0.8% (the aggregate model over-forecasts Feb-Mar on the full data).

**Why the opposite of the screen (r124):** on the 3-store screen the bottom-up aggregates are noisy (few series to average) so the light aggregate model adds value; on the full 30,490-series data the recipe's bottom-up aggregates are already excellent, so the same light aggregate model is WORSE than bottom-up and reconciling toward it hurts everywhere.

**Verdict:** discard. Middle-out reconciliation with a light aggregate model does not close the leaderboard gap — it hurts on the full data. The reconciliation machinery is correct (the screen proved the mechanism); the limitation is that the aggregate model cannot beat a 30,490-series bottom-up.
