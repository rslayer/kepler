# r069 — lgbm_xmas0_tgd_r3_noitem

**Author:** researcher (session researcher-20260913-2)

**Hypothesis:** H050 — drop the `item_id` categorical on top of r061, so a series' level reaches
the model only through its as-of-origin lags and rolling means. If item_id memorises per-item
constants from the training window, removing it should help the folds where levels drift
(fold 6 bias -0.019) and explain why older training rows hurt February.

**What changed:** `src/model.py`: new class `LGBMNoItemId` (`lgbm_xmas0_tgd_r3_noitem`) = r061
features without `item_id`. Nothing else.

**Result vs current best (r061, 0.803303):** WRMSSE 0.809057 (spread 0.000413), paired gain
-0.005754. Per fold (r061 -> r069): 1 0.8236 -> 0.8297 (+0.0061), 2 0.8142 -> 0.8151
(+0.0009), 3 0.8351 -> 0.8315 (-0.0036), 4 0.7854 -> 0.7947 (+0.0093), 5 0.7942 -> 0.8002
(+0.0060), 6 0.8016 -> 0.8084 (+0.0069), 7 0.7905 -> 0.8032 (+0.0127), 8 0.7819 -> 0.7896
(+0.0077). Bias -0.0045 -> -0.0254 (folds 1/2/4 about -0.05). WAPE flat (0.7699 -> 0.7701).
Keep rule: 1 FAIL, 2 FAIL (6 folds), 3 FAIL. Detail in runs/detail/r069.json.

item_id carries real per-item level: without it the model regresses big sellers toward the
pooled mean and under-forecasts by 2.5%. The useful by-product is the fold pattern: fold 7 is
again the largest loser (+0.013), the same fold that every long-history run (r065-r067) hurt
by +0.002 to +0.014. Fold 7 is the fold most dependent on an up-to-date per-item level, which
is exactly what older training rows dilute.

**Verdict:** discard. Change reverted.
