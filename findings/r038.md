# r038 — lgbm_xmas0_o80

**Author:** researcher (session researcher-20260912-1)

**Hypothesis:** H005 — 80 weekly training origins (~560 days) instead of 40, on top of r037.
Four v0 runs (r008, r011, r013, r024) cut the December fold by 0.03-0.04 and lost 0.002-0.009
on the February fold; the v1 keep rule tolerates a fold loss inside that fold's seed spread,
so this is the first three-seed test of the long-history lever.

**What changed:** `src/model.py`: new class `LGBMXmasOrigins80` (`lgbm_xmas0_o80`), a
subclass of `LGBMChristmasZero` with `N_TRAIN_ORIGINS = 80`. Nothing else changed; features
and params identical to r037.

**Result vs current best (r037, 0.805456):** WRMSSE 0.801983 (spread 0.001924, fifteen times
r037's 0.000128), paired gain +0.003473 against a threshold of 0.003848. Per fold
(r037 -> r038): 1 0.8302 -> 0.8254 (-0.0048), 2 0.8165 -> 0.8085 (-0.0080),
3 0.8342 -> 0.8173 (-0.0169), 4 0.7889 -> 0.7852 (-0.0037), 5 0.7955 -> 0.7938 (-0.0017),
6 0.8028 -> 0.8048 (+0.0020), 7 0.7896 -> 0.7934 (+0.0037), 8 0.7859 -> 0.7876 (+0.0017).
Same shape as v0: November-January folds gain, February folds lose. WAPE h15-28 0.7848 ->
0.7810; bias +0.0007 -> +0.0009. Keep rule: 1 paired gain FAIL (gain below 2 x max spread,
and the max spread is the child's own), 2 no fold regress FAIL (fold 7 +0.003722 vs tolerance
0.002253), 3 bias PASS. Detail in runs/detail/r038.json.

Two things worth recording: the 80-origin model is much noisier across seeds than the
40-origin one (0.0019 vs 0.0001), which by itself doubles the paired-gain threshold; and the
losses sit on folds 6-8 (origins in February), consistent with the v0 fold-3 loss. H022
(wider spacing) and H023 (short recency half-life) are the named ways to keep the December
gain without the February loss.

**Verdict:** discard. Change reverted.
