# r071 — lgbm_xmas0_tgd_r3_o80hl120_ev

**Author:** researcher (session researcher-20260913-2)

**Hypothesis:** H051 — the primary calendar event name as a horizon-varying feature on top of
r070 (the unconsumed part of H029). With 80 weekly training origins every named event appears in
training at least once, so per-event effects (Super Bowl, Valentine's Day, Presidents Day,
Easter...) should be learnable where the 40-origin model only had the generic `event_flag`.

**What changed:** `src/features.py`: `Panel.__init__` adds an integer-code column `<col>_code`
(-1 = empty, sorted-label order) for every text calendar column; they are numeric, so they are
merged into every frame as available flags but no existing model consumes them (r070's config
hash is unchanged). `src/model.py`: new class `LGBMEventName`
(`lgbm_xmas0_tgd_r3_o80hl120_ev`) = r070 features + `event_name_1_code` (numeric codes).

**Result vs current best (r070, 0.798186):** WRMSSE 0.798295 (spread 0.000989), paired gain
-0.000109 against a threshold of 0.002770. Per fold (r070 -> r071): 1 0.8221 -> 0.8224
(+0.0003), 2 0.8038 -> 0.8033 (-0.0006), 3 0.8195 -> 0.8192 (-0.0003), 4 0.7767 -> 0.7782
(+0.0015), 5 0.7906 -> 0.7912 (+0.0006), 6 0.7998 -> 0.8007 (+0.0009), 7 0.7898 -> 0.7888
(-0.0010), 8 0.7833 -> 0.7827 (-0.0006). Bias -0.0090 -> -0.0082; WAPE unchanged. Keep rule:
1 FAIL, 2 PASS (8/8), 3 PASS. Detail in runs/detail/r071.json. The run took 1097 s against
r070's 667 s (wider frames from the added code columns, and possibly machine load).

Neutral. One occurrence per event, at a recency weight of ~0.12 for last year's, is not enough
for the trees to learn an event-specific effect beyond event_flag; the events that matter for
WRMSSE on this store (Thanksgiving week, Christmas) are already handled by postprocess and
weighted history.

**Verdict:** discard. Change reverted.
