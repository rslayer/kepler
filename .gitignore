# --- Frozen / secret / regenerable ---
data/*/raw/
holdout/
.env
*.kaggle*

# Snapshot parquet is regenerated by `make data` (git-lfs not in use here).
# MANIFEST.txt IS committed - it is the reproducibility anchor.
data/*/snapshot/*.parquet

# --- Python ---
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/

# --- OS ---
.DS_Store
adversary/reruns/
runs/runs.v0.bak
runs/runs.partc.bak
runs/runs.v1.bak
runs/runs.v1-partc.bak
runs/runs.v2.bak
runs/cycle-logs/
runs/.cycle.lock/
runs/runs.v3.bak
data/*/raw
