"""Feature construction. The researcher edits this file.

Contract: every builder receives the fold `origin` date explicitly and may use only
information strictly before it. Nothing at or after `origin` may enter a feature.

Filled in during Phase 1.
"""
