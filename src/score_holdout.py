"""HUMAN ONLY: one-shot scoring of a model against holdout/. FROZEN.

Not in the SPEC layout; added because `make score-holdout` needs an entry point that is
separate from the agent-visible harness. Writes runs/holdout.csv.

Filled in during Phase 1.
"""
